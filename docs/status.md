# Статус этапов

> Часть спецификации Правки. Была разделом README; правится вместе с кодом режима. Карта репозитория — `CLAUDE.md`, короткий обзор — `README.md`.

- [x] **Этап 0** — сборка, подпись, автоверсия, доставка APK
- [x] Этап 1 — каркас и Claude API
- [x] Этап 2 — служба доступности и системная кнопка
- [x] Этап 3 — плавающая кнопка
- [x] Этап 4 — Quick Tap и плитка
- [x] Этап 5 — словарь
- [x] Этап 6 — редактор промптов
- [ ] Этап 7 — Gemini Nano (не делаем: Сонет справляется, локальная модель не нужна)
- [x] Этап 8 — роутер, настройки, диагностика
- [x] Засечка 1.0 — лента, категории, автоматика, зеркала
- [x] Засечка 2.0 — ценность часа и балл дня, 24 часа, копии и восстановление,
      дела из Todoist
- [x] Разноска 1.0 — наговор → дела в Todoist: разбор Опусом, плашка,
      правка дела руками, защита от дублей и от потери разбора
- [x] Разноска 1.1 — решение по каждому делу на плашке, разбор чужого текста
      (буфер, выделение), маршруты на его поправках, отмена отправки
- [x] Спорт 1.0 — тренировки, здоровье и форма из intervals.icu в кэш,
      готовность на телефоне, графики, вопрос по своим тренировкам Опусу
- [x] Еда 1.0 — четыре дороги ввода (голос, текст, снимок, штрихкод), КБЖУ
      Сонетом, дневник с целями, приписка к ленте и итог дня в wellness
- [x] Тело 1.0 — журнал силовых голосом с прошлым разом и дельтой, карточка
      дня из календаря, светофор по его железным правилам, GTG-стрик, таймер
      отдыха, роутер намерения на одной кнопке «Т», справочники статическим
      файлом, запись подходов в активность Garmin, сводка для чата
- [x] Тело 1.2 — аудит: пять багов (затирание описания активности, раскол
      дня силовой, потеря feel, отравление лучшего виса, кэш промпта),
      карточки КПД и «Цели октября», вердикт пробежкам по его правилам,
      прогрессия упражнения, уведомление о приехавшей тренировке с feel из
      шторки, автогалочки в Notion-Дневник, список неразобранного
- [x] Тело 1.1 — правила из Notion читаются по-настоящему (хаб как источник,
      поиск страницы блока, внятная диагностика доступа), все настройки в одной
      вкладке по группам режимов, «Мой рацион» тапом, зарядка одной понятной
      отметкой, видимый маршрут подходов в Garmin, правила блока в контексте
      вопроса тренеру
- [x] Засечка 2.1 — параллельный трек: то, что шло поверх дела, ничего у него
      не отнимает; врезки телефона вернулись вторым слоем; «и параллельно…»
      одной фразой; две колонки времени в выгрузках
- [x] Самообновление — раз в сутки смотрит `apk-builds`, качает и ставит,
      сверяя свою ветку (по образцу Слушалки)
- [x] Тело 1.3 — вкладка видит ВСЕ сессии дня и ВСЕ пункты плана: «Турник +
      пресс» больше не зарядка, чек-лист на каждую силовую, списки в одну
      строку и текст после списка, один парсер строки плана с JVM-тестами на
      настоящих описаниях, справочник упражнений живой из базы Notion,
      страница «План v3» вместо архивного «Блока», снимок справочника от 04.09
- [x] Карта и распил (04.09) — `CLAUDE.md` и `docs/<режим>.md` вместо README на
      две тысячи строк; `ClaudeProvider` разрезан на транспорт и разборы режимов
      (`Claude<Режим>.kt`), тексты промптов по режимам (`core/prompts/`), служба
      доступности отдала режимные куски в `Service<Режим>.kt`; план
      Gradle-модулей — `docs/architecture.md`
- [x] Засечка 2.3 — параллельный трек закрыт (05.09): «эксперимент оказался
      неудачным, засоряет ленту»; телефон считается по дням — строка «Телефон»
      у итогов (YouTube, Telegram, Claude, звонки с собеседниками, экран),
      досчёт прошлых дней из суточных сумм; записи второго трека уехали в
      архив, не стёрты
- [x] Notion 2.1 — только сырые данные (05.09, второй заход после «куча
      лишних таблиц»): базы «Засечка» (лента с очками), «Еда», «Тренировки»
      (км, темп, GAP, зоны, decoupling), «Силовые», «Зарядка», «Телефон» (по
      дням), «Форма» (wellness по дням), справочник «Категории»; агрегатная
      база «Дни» в корзине, служебные строки убраны, аналитические базы — в
      подстранице «Разбор»; итоги по неделям и месяцам — представления и
      графики Notion по колонкам «Месяц»/«Неделя»
- [x] Notion 2.2 — синк, который видно (07.09): 404 на страницу заводит её
      заново, а не останавливает базу; база без карты пропускается на обход,
      а не валит его; 429 и сеть ждут тика без шума; любой срыв — в состояние
      словами; состояние всегда с временем последней записи. Схема 6: время
      по-экселевски — «Начало» и «Конец» датами вместо диапазона «Дата» у дела
      и тренировки, для глаз «Когда» и «Время» коротко; «Оценка» в
      Засечке, «Уверенность» в Еде, жиры и углеводы в Форме, «Галочек» в
      Силовых и сессии по галочкам чек-листа, дубль «Дата»/«День» в Силовых
      и Зарядке убран, завтрашний прогноз CTL/ATL строкой не становится,
      названия и описания баз выравниваются по коду
- [x] Notion 2.0 — структура жизни в коде (`core/NotionLifeSchema.kt`):
      приложение создаёт базы, достраивает колонки, переезжает со старой
      раскладки и убирает устаревшие колонки само
- [x] Засечка 2.5 — «Записать мысль» (09.09): тёмная первая кнопка меню «З»
      диктует мысль в комментарий идущего или последнего дела; помидоры сняты
      целиком; экран не гаснет во время любого тейка (окно-хранитель службы)
- [x] Выгрузка 1.0 — вся жизнь одной книгой Excel (09.09): лист на базу Notion
      из общего с синком сборщика строк, даты датами, свежее сверху; CSV
      ленты, еды и всей жизни, сводки для чата и «Запрос для чата» сняты
- [x] Засечка 2.4 — комментарии к делу (06.09): баббл 💬 в ленте рядом с ✎ и ✕,
      поле в редакторе и своё окно, пункт «Комментарий к «…»» в меню долгого
      нажатия «З» с диктовкой через движок Правки, колонка «Комментарий» в
      Notion (схема 4), `comment` в CSV и вебхуке Sheets
- [x] Засечка 2.2 — автопилот заговорил (05.09): приезд закрывает дорогу и
      спрашивает «что теперь?», переезд с устаревшим делом в ленте узнаётся по
      зафиксированному отъезду, места «по видимости» читают свежие сканы (свои и
      системные), машина по Bluetooth сразу начинает поездку с кнопкой
      «Отменить» и узнаётся по адресу, пуши каналом HIGH, блокеры уведомлений и
      Bluetooth с кнопками; метка NFC срабатывает на заблокированном экране
      (`showWhenLocked`); правила автопилота — `core/AutoPilotRules.kt` с тестами
- [x] Модели 1.0 — модель и усилие каждой дороги в Claude выбираются в
      настройках (группа «Модели», 06.09): десять дорог по режимам, заводские —
      как были зашиты, Fable 5.1 в каталоге цен (и Сонет 5 по своей цене),
      правило про размышления — `provider/RequestPolicy.kt` с тестами
- [x] Каркас 2.0 — редизайн экранов (15.09): одна шапка на все вкладки
      (пиктограмма, название с полоской, статистика · «$» · шестерёнка своей
      группы настроек), фон со знаками режима и плашки с заголовками,
      пояснения под названиями сняты; страницы поверх вкладок с «назад»;
      стоимость API одним экраном по всем дорогам (добавлен учёт сверки
      паттернов); Правка — нерасшифрованные записи сверху, последние 20 и
      «Показать всё», статистика диктовки с выгрузкой по периоду; Засечка —
      без День/Неделя и отмены, «Баланс +11 = +12 − 2» по центру, Телефон
      плашкой с тапом «считать», микрофон в редакторе записи правит её
      голосом; Отчёт → «Общая статистика» с книгой Excel за значком; Паттерны
      и их ночной поиск удалены; обучение только по кнопке; штриховые значки
      микрофона. Второй заход того же дня: узор знаков режима на плашках
      вместо фона, фон темнее, лента без плашки, «Спорт»/«Еда» вместо «Тело
      (С)/(Е)», вилка и нож вместо тарелки; чинится обрыв стрима в поле
      (замок на preview/write); режим отладки с логом запросов целиком;
      правка одного слова руками — в словарь без модели (`core/EditDiff.kt`);
      разделы словаря сворачиваются. Третий заход: захват правок — окно
      подписки на события текста после доставки, разбор по тишине; журнал
      правок «надиктовано → модель → владелец» (`data/CorrectionsLog.kt`) с
      карточкой в Обучении и CSV в выгрузке (служебная часть этого захода
      легла в код только четвёртым заходом — см. урок в agreements). Четвёртый
      заход: чтение поля раз в 300 мс и разбор по опустевшему полю; узор
      «развалинами» — крупные повёрнутые знаки; тикер диктовки — бегущая
      строка (`trigger/MarqueeTickerView.kt`) у всех четырёх кнопок с общей
      шириной в настройках; подсказки распознавателю — только верные формы,
      не больше сорока, с тумблером и замером задержки. Пятый заход (16.09):
      чужой текст — текстбокс «причесать текст» первым на вкладке Правки
      (результат в буфер) и «Чистка буфера» в меню «П» (вставка чужим
      приложением по `ACTION_PASTE`, границы вставки — `core/TextSpans.kt` с
      тестами, чистка по выделению со стримом в поле)
- [x] Отчёт 1.0 — день в графиках (07.09): балл на радуге с местом среди своих
      дней и тем же днём недели за пять недель, плитки с дельтой к тому же дню
      неделю назад, донат и «что заняло больше всего», день по часам двумя
      полосами, треугольник ценности, сравнение по категориям и телу, неделя
      стопкой и семь суток по часам, балл за 28 дней, телефон, тело, еда, дела
      и Правка, коэффициенты с формулами; числа — `core/DayReport.kt` с
      JVM-тестами, идущий день сравнивается с прошлым до того же часа
- [x] Засечка 2.5 — автопилот сшивает швы (08.09, владелец включил
      уведомления и увидел автопилот целиком): машина после отъезда — одна
      дорога с момента потери сети, «Сказать» из любого пуша несёт якорь
      времени (сказанное ложится с момента шва), у места есть дело по приезду
      («Летово» → «Забираю Серёжу»), вопросы, на которые ответила сеть или
      машина, снимаются вместе с пушем; тап по «не размечено» и по «··· без
      записи» — сказать, набрать, к предыдущему, к следующему
- [x] Уведомления после обновления (08.09): служба пишет в журнал сборку,
      обновление и состояние уведомлений при каждом подъёме, выключенные —
      просит заново раз на сборку системным диалогом (`NotifPermissionActivity`),
      записка на «З», плашка «Разрешить» в приложении и автозапрос при первом
      открытии на новой сборке
- [x] Обучение без суда (08.09): ночное самообучение Засечки снято целиком
      вместе с ⭐ над «З» (набор правил остался, правится руками); обучение
      Правки возвращает только словарь — замену одного слова другим (HARD /
      HINT / PROTECT) — и кладёт её сразу, правил не предлагает, недельная
      автооптимизация снята; итоги дня Засечки — без сна, «чистое время» и
      доля каждой категории от бодрствования между временем и очками
      (`core/WakingShare.kt` с тестами)
- [x] Микрофон 1.0 — плашка между «П» и «З» (13.09): кто слушает диктовку,
      телефон или Bluetooth-гарнитура, показывает и переключает плашка на
      ширину кнопки и в половину её высоты (кружок в треть не попадался); гарнитура — канал SCO поднимается перед тейком и опускается после,
      Google-сессия ждёт канал; геометрия стопки — `core/StackGeometry.kt` с
      тестами. Заглушка до помощника с кнопки гарнитуры Shokz — кнопка
      сделана 25.09, см. ниже
- [x] Витамины и элементы (13.09): двадцать ключевых веществ
      (`core/Micronutrients.kt`) считаются вместе с КБЖУ — моделью, точно по
      рациону и по штрихкоду; во вкладке «Еда» полоска на вещество со
      светофором (мало / средне / достаточно / перебор), засечкой суточной
      нормы внутри шкалы и строкой «зачем он нужен» под ней; свёрнутая
      карточка показывает только дефициты. Выпитые таблетки — той же кнопкой
      «Т» («выпил витамин D две тысячи и магний»): позиция с пометкой
      `pill`, без калорий, вид приёма «добавки», и в сутках видно, что из
      тарелки, а что из банки. Наружу — база Notion «Витамины» (сутки одной
      строкой, колонка на вещество) и справочник «Нормы»; обе едут в книгу
      Excel своими листами. Схема Notion 6 → 7
- [x] Диск кнопок (19.09): четыре плавающие кнопки по кольцу вокруг
      шестерёнки (`trigger/DiskController.kt`, `core/DiskGeometry.kt` с
      тестами) — крутится за любую кнопку со щелчками по четвертям и
      броском, переезжает за стекло или за шестерёнку по долгому нажатию,
      у края докуется до середины шестерёнки («П» и «З» внутри, «Д» и «Е»
      за краем), домой по простою; шестерёнка настоящая и крутится с диском,
      двойной тап по ней — всё в точку «П»; окна за краем снимаются. Выбор
      «Круг · Стопка» в Общих, стопка живая. Третий заход: «П» сверху, «З»
      снизу (раскладка зеркальна у краёв), «Е» выключена с завода, связка
      списком (`RingButton`, `chain()`); четвёртый: серый кружок настроек
      снят («отвратительно»), веер — тап по шестерёнке и двойной тап по
      стеклу, «Настройки» в меню долгого нажатия каждой кнопки; двумя
      пальцами диск везут, где бы ни взяли (пальцы со всех окон в
      `DiskController.finger`); мягкая тень под стеклом (шкала-метки сняты:
      «залезают на круги»), долгое нажатие на стекло выдвигает диск целиком
      и обратно, автоуборка к ближайшему краю по простою (свой тумблер);
      пятый: стекло стало светлым («а то теряется иногда») с тумблером
      «Светлее · Темнее» в Общих и фаской по кромке, кнопки, шестерёнка и
      кружки веера — выпуклые (`trigger/BubbleSkin.kt`, числа —
      `core/DiskLook.kt` с тестами); шестой, следом: кнопки на диске плотнее
      («выглядит так, что диск над кнопками, а не наоборот»), стекло рисует
      под ними тени, шестерёнка посреди диска — без подложки и цветом по
      стеклу, нажатие гасит блик; седьмой: весь вид диска — ползунками в
      Общих («и нужно всё это в настройки: прозрачность, размер») — размер
      диска и шестерёнки, плотности стекла, кнопок и теней, кнопка сброса;
      плотности хранятся как «не трогали» и до первого касания считаются;
      восьмой (20.09): плашки в том же материале, что кнопки — бегущие
      строки, меню, записки, «отмена», ручка стопки; `BubbleSkin` умеет обе
      формы и берёт её у себя же; девятый (20.09, по списку владельца):
      матовое стекло зерном (системного размытия под кругом не бывает), свет
      сверху на тарелке, рельс под кнопками, инерция качения — три тумблера и
      ползунок в «Виде диска»; **прогресс по кромке стекла** от своей истории
      (`core/Pace.kt` + `data/PaceStore.kt`, прямая «основание + наклон ×
      символы» на пару «дорога + модель», у края — по видимому полукругу);
      кнопка пульсирует в ритме голоса (`core/MicLevel.kt`); ночная палитра
      поднята и почищена, нижняя панель — на фоне с волосяной линией, у
      плашек приложения та же фаска, что у кнопок; утопание диска после
      долгого простоя с пробуждением по тапу (свой тумблер, срок и глубина);
      десятый (20.09): стрелка на кромке (убрать досрочно · достать), дуга
      прогресса в материале кромки (канавка · градиент · блик), калибровка
      ожидания (к сроку 90 % вместо 75 %, прямая с четвёртого замера) и
      строка «Что знает дуга» с числами по каждой дороге; плашки приложения
      со своей темнотой и слоями стекла (`ui/CardLook.kt`); все настройки
      вида — в новой группе «Внешний вид»; одиннадцатый (20.09): дуга берёт
      прошлое из журнала правок вместо учёбы с нуля (`PaceStore.seedFromHistory`,
      один раз; дороги без журнала — с прикидки по тому же журналу,
      `core/PaceSeed.kt`, тающей к дюжине своих замеров), полоса шире и
      выпуклая кольцами поперёк (`DiskLook.bandTone`) вместо цветной дуги с
      белой ниткой
- [x] Путь распознавания Google (16.09): офлайн-пакет (заводское) или
      системный с сетью — дорога клавиатуры Google на русском; подсказки
      движку — слова владельца впереди семени (`core/BiasingList.kt`).
- [x] Выгрузки для разбора наверху «Логов» (16.09): расшифровки, история
      правок, правки руками, метрики — одной кнопкой каждая.
- [x] Разбор журналов (16.09): семя словаря v3 — имена героев и термины из
      1490 тейков, снятие ошибочных HARD; правка формы слова — в очередь, не
      в словарь; словарь и правила выгружаются из «Логов».

- [x] Пунктуация по разбору (16.09): точки вместо цепочек запятых в
      переписке, запрет удалять «не»/«нет» в CLEAN; ResponseCleaner режет
      ёлочки и «мысли вслух» модели.

- [x] Правила обучения (16.09): блок в промпт под тумблером, заводское
      выключен; потолок виден во вкладке Обучение. Семя v3 снимает с телефона
      HARD, ломавшие живые слова, и подсказку «та → ТА».
- [x] Ночной разбор (16.09): Fable 5.1 батчем по журналам дня и недели,
      второй проход проверяет, высокое применяется само с откатом, отчёт и
      ответ текстом в настройках Правки.
- [x] Кэш промпта (16.09): картинка после кэшируемой головы (Еда по фото),
      Разноска под кэшем, свидетельства ночного разбора одним системным
      блоком под кэшем, счётчики кэша на экране стоимости.
- [x] «Ещё → Разборы» (16.09): ночной разбор и тень переехали из настроек
      Правки своей плашкой; обрезанный по длине ответ батча — сбой с причиной,
      запас под мысли в батче 32 тысячи.
- [x] Тень второй модели (16.09): те же диктовки ночью через Опус батчем,
      слепой судья Fable 5.1 high, счёт и деньги в карточке; первая ночь —
      до 200 диктовок за месяц, дальше за сутки.
- [x] Эвалы батчем (16.09): золотой набор одним Message Batch в форме дневной
      чистки, батч в пути переживает смерть процесса.
- [x] Разборы группами (17.09): День / Неделя / Тень / История, словарь по
      видам, правила, идеи по приложению; всё high применяется само, кроме
      отклонённого проверкой.

- [x] Разборы чипами (17.09): Применено / Предложено / Идеи с галкой и
      крестиком, «Принять все», возвращённое снова к решению, советы к идеям,
      лог для Claude Code, прогресс батча с «Проверить сейчас» и «Отменить»;
      low отбрасывается, дневной прогон правит словарь только по словам дня.
- [x] Тень снята (18.09): движок, тумблеры, кнопки, дорога SHADOW_CLEAN
      удалены; NightSweep на старте закрывает её прогоны и гасит осиротевшие
      батчи у Anthropic; кнопка «Проверить батчи» в «Ночью».
- [x] Опус для чистки, тень выключена (18.09): по тени 17:2; разовая
      миграция явного Сонета на Опус.
- [x] Стекло кнопок 2.0 (18.09): шестерёнка над «П» с веером быстрых
      настроек (модель чистки Опус/Сонет, микрофон телефон/гарнитура,
      проверка и установка обновления, «спрятать всё» в красную точку с «П»,
      тап по точке возвращает); три точки и плашка микрофона сняты; связка
      едет на пружинах бусами (`core/ChainPhysics.kt` с тестами), кнопки
      сжимаются под пальцем; серая «отмена» у всех четырёх записей под
      бегущей строкой; прозрачность шестерёнки, веера, ручки и «отмены» — как
      у кнопок.
- [x] Сравнение моделей вручную (18.09): плашка в «Разборах» — период
      (сутки / 3 дня / неделя), потолок диктовок (10 / 20 / 40), ориентир
      цены, кнопка «Сравнить»; три плеча Сонет · Опус · Опус low чистят
      кусками за тик, слепой судья троек батчем, счёт, скорость, деньги,
      изъяны, примеры; автостарта нет, дорога `compare` на экране «$».
- [x] Деньги по дорогам (17.09): ведро на дорогу и день в Stats, карточка «по
      дорогам, семь дней» на экране «$» с моделью и долей; тень и измерение
      промпта — путём дневной чистки с сохранением после каждой диктовки,
      чтение прогона под замком; тень раз в неделю по умолчанию; правила
      блока Notion — метка «раз в сутки» в настройках; потолок дня для
      автоматов ($4), «Отменить» останавливает идущий цикл.
- [x] Табло «Что работает» и журнал night.log (17.09): по строке на автомат
      с итогом, сбоем, стадией и следующим запуском, пульс службы, кнопки под
      идущими; табло и журнал — в логе для Claude Code.
- [x] Недельная правка промпта (17.09): в ночь на субботу Fable по идеям
      недели предлагает точечную правку CLEAN, новый промпт перечищает
      диктовки недели, слепой судья и близость к правкам руками решают,
      версии с метриками в приложении, откат через неделю и кнопкой.
- [x] Тень и эвал обычными запросами (17.09): батчи Опуса висли на «0 из N»,
      чистка идёт кусками по 25 за тик с сохранением, судья остаётся батчем;
      застрявший батч старой сборки отменяется сам.
- [x] Первый круг по логу разбора (17.09): шесть идей ночи — в промпт CLEAN
      (выдуманные слова, «не», зачин реплики, союзы автора, искажённые
      маркеры списка, словарь по контексту и падежу), блок PROTECT с
      падежами, семя v4 без HINT «поле / более → Полли».
- [x] Ночной разбор дешевле (18.09): третий проход снят в код
      (`consistencyHolds`), заводское усилие разбора и проверки — medium,
      проверке не возят промпт CLEAN, в пятницу только недельный, проверка
      без ответа ничего не применяет, потолок дня — по вчера/сегодня; на
      экране «$» батчи подписаны «уже со скидкой».
- [x] Проверка кэша по дорогам (18.09): каждый запрос уезжает с ключом дороги,
      под строкой дороги на «$» — вход и доля из кэша (ноль при большом входе
      красным), в логе запросов — строка ответа с usage и ценой.
- [x] Только недельный с завода (18.09): тумблер «Сутки каждую ночь» в плашке
      «Ночью», выключен; недельный в ночь на пятницу всегда, кнопка «Сутки»
      независима; табло говорит «по кнопке».
- [x] Микрофон слышит с первого слова (22.09): в манифесте не было
      `MODIFY_AUDIO_SETTINGS`, и система МОЛЧА отказывала в смене маршрута —
      в машине хендс-фри держал канал, а Правка слушала салон; теперь маршрут
      забирается по-настоящему и ждёт переезда, движок будится ещё под пальцем
      (`warmUp`), а глухое окно названо словами в бегущей строке («секунду…» →
      «говори»). Рядом — «Перезагрузить микрофон»: Настройки → Общее и долгое
      нажатие на кружок микрофона в веере; бьёт по маршруту, службе
      распознавания и залипшему удержанию и рассказывает, что нашла.
- [x] Облачный Google — главный путь (22.09): заводское переключено на сеть и
      разовой миграцией переведён уже стоящий телефон; служба на этом пути
      называется явно пакетом Google, потому что «сеть» в настройке ещё не
      значила «Google» — системная по умолчанию на части телефонов своя. Кто
      распознаёт, теперь видно строкой «Служба» в настройках и в журнале.
      «Секунду…» и приглашение говорить — у всех четырёх кнопок.
- [x] Убранный диск отдаёт кнопки (22.09): автоуборка больше не докует диск
      половиной, а прячет ЕГО — стекло уходит за край до краешка со стрелкой,
      «П» и «З» выдавливаются из него и остаются на экране целиком
      (`DiskGeometry.extrusion` под тестами, своя пружина на кольцо). Стрелка
      получила три исхода вместо одного: у убранного диска тап выводит
      половину, второй тап подряд — весь; у выехавшего — убирает. Утопание
      осталось вторым этажом над уборкой и ведёт за край уже выдавленные
      кнопки; его старая ветка «дотянуть диск с середины экрана» снята вместе
      со стрелкой, которая её требовала. Следом: стекло ТЯНЕТСЯ за кнопками
      («надо чтобы они выдавливались вместе с краем!») — контур считается
      лучами и растягивается под каждый карман (`DiskGeometry.blob` под
      тестами), у невыдавленного диска это прежний круг; тень пересобрана
      (ровное тело плюс бахрома снаружи: два градиента внахлёст давали пятно
      под перемычкой). И починен двойной тап: первый тап двигает мишень, так
      что окно «ещё» считается от времени, а не от места.
- [x] Диск без рывков (22.09, вечер): окно стекла не меняет размер никогда и
      стоит по центру кольца, тарелка нарисована в нём сдвинутой на
      выдавливание — уши и прыжки на стрелке ушли вместе с ресайзом. Новая
      цель подхватывает скорость пружин (двойной тап по стрелке — одно
      движение). Убранный диск за стекло везут по краю, и он остаётся с
      ушками; стекло берут по живому контуру с плечами растяжки и тенью. Вис
      «за шкирку» прожил одну сборку и снят по просьбе владельца («классная
      тема, но мешает»).
- [x] Кнопку ведут по направлению (22.09, вечер): вертикаль прочь от центра —
      диск едет по краю (убранный остаётся с ушками), к центру — крутится,
      горизонталь и диагональ — отцепляется и едет целиком за кнопкой, кнопки
      садятся в стекло быстрой пружиной (`DiskGeometry.pull`,
      `DiskPhysics.quick`, под тестами). Утопание через пять минут снято
      целиком вместе с тумблером и ползунками.
- [x] Опус 5.5 (22.09): все дороги Опуса 5 и Fable — на `claude-opus-5-5`
      ($4/$20), усилия явно: правка medium, Засечка и Еда xhigh, спорт
      medium, Дела high, разборы (ночной, проверка, правка промпта, судья,
      сверка паттернов) — max батчами. Еда отделилась от спорта дорогой
      `FOOD`; разовая миграция снимает прежний явный Опус/Fable; запас под
      мысли растёт с усилием (`RequestPolicy`, под тестами).
- [x] Ряд усилия в меню «П» (22.09): medium · high · xhigh над столбиком,
      выбор постоянный — настройка дороги `PRAVKA`.
- [x] Проверка расходов и дуга по усилию (22.09): цена батча — по модели из
      ответа (`ClaudeBatches.costUsd`), чтение кэша Опуса 5.5 — $0.20; дуга
      считает по тройке «дорога + модель + усилие» с множителем усилия в
      прикидке (`PaceSeed.effortFactor`).
- [x] Пауза не кончает диктовку (23.09): тишина больше не закрывает тейк —
      непрерывная сессия поднимается заново тем же клиентом, тишинные ошибки
      не ведут к «сдаться», кончает запись кнопка. Страховка от случайного
      нажатия — десять минут без нового слова, считая от последнего слова;
      от петли «кончилась — подняли» стоит счёт срывов. `core/ListenPolicy.kt`
      под тестами, правило общее для всех четырёх кнопок.
- [x] Деньги (23.09): кнопка «₽» четвёртой на диске (контроллер «Д» со своим
      лицом), седьмая вкладка внизу, траты голосом с двойной проверкой суммы
      (слова + код), выписки Тинькова (CSV), Альфы (CSV), МКБ (.xlsx) и чат
      «Плати по миру» — разбор кодом до копейки, сверка голоса с выпиской и
      переводов Саша ↔ Марианна, вопросы группами с запоминанием в
      справочник, подсказки Опуса, тумблер «+ ЗФ», книга «Деньги» (.xlsx).
      Справочник получателей — заводской в assets (решение владельца) плюс
      свои правила на телефоне поверх; наличные МКБ по 23 000 — няня.
      Дальше: Google Sheets, приложение у Марианны и общая база.
- [x] Деньги 1.1 (23.09): без копеек на экране, неделя и месяц с
      «ушло» красным / «пришло» зелёным и сравнением, плитки темпа и
      прогноза, вопросы карточками («Не знаю» · «Сказать» голосом → Claude),
      категории с раскрытием по магазинам, дни и полгода графиками,
      регулярные платежи, «спросить Claude» и паттерны года (Опус 5.5).
- [x] Деньги 1.2 (23.09): пуши Т-Банка и чата «Плати по миру» — своя
      служба уведомлений, разбор кодом под тестами на настоящих текстах,
      выписка заменяет пуш с наследованием категории, «отменили?» для пуша
      без пары, тумблер и состояние доступа в настройках.
- [x] Деньги 1.3 (23.09): голос вкладки — наш движок (был системный Google),
      ДДС по месяцам в три раздела, баланс от якорей (снимок владельца,
      вписанное, «Доступно» из пушей).
- [x] Деньги 1.4 (23.09): наличные — свой счёт, записи со слов владельца,
      доля и долг Наташе, карточка «Счета», кнопки «Личное · ЗФ» с ВГО.
- [x] Деньги 1.5 (23.09): выписка Т-Бизнеса (счёт ЗФ) — разбор, правила
      «ЗФ:», склейка выплат владельцу, пуши карты ЗФ *8958.
- [x] Деньги 1.6 (23.09): займы ЗФ владельцу — займы с обеих сторон,
      касса ЗФ (наличные от клиента), остаток ЗФ.
- [x] Деньги 1.7 (23.09): выгрузка «все карты», округления в копилку,
      якорь Black Premium на снимке, размерность — рубли без копеек.
- [x] Второе издание (24.09): один набор деталей `ui/Kit.kt` и значки
      `ui/Glyphs.kt` штрихом режимов по образцу Слушалки; вкладка в краске
      своей кнопки; все 26 окон — листами снизу; пояснения — за «i»; одни
      поля экрана; шапка без полоски, статистика круговой диаграммой.
      Настройки — пять полок (Режимы · Голос и Claude · Подключения · Кнопки
      и вид · Приложение) со строками-состояниями, «Модели» — сводками,
      «Настройки» в меню кнопки открывают её группу. Спорт — «Сегодня · Путь
      · Журнал», Деньги — «Сводка · Разобрать · Журнал», лента Засечки —
      лист записи по тапу, «Ещё» — две полки со значками, служебные экраны —
      на плашках.
- [x] База в папке (24.09): вся база приложения — файлы одной папки
      (`DataRoot`): в памяти приложения или, по кнопке «Настройки → База
      данных», в `Documents/Pravka`, которую видно и можно скопировать.
      Переезд в два шага с закреплением на старте, открытие базы из папки на
      новом телефоне или после переустановки, «база недоступна» словами и
      уведомлением. Сторож `DataRootGuardTest`: ни одного стора мимо папки.
- [x] Профиль и режимы (25.09): первый запуск спрашивает, кто пользуется
      (Саша · Марианна · Серёжа · другой с именем и родом), тумблеры режимов
      Засечка · Дела · Спорт · Еда · Деньги — выключенный пропадает снизу, со
      стекла, из настроек и из фона. Не владельцу: род в чистке, свои траты,
      без заводских Денег владельца, нейтральные категории ленты, ночные
      автоматы и Notion выключены с завода.
- [x] Копия раз в сутки (25.09): zip всей базы ночью в
      `Documents/Pravka-backups`, две недели и по месяцу за год.
- [ ] Остатки владельца в APK: словарь-сид с его семьёй и ЗФ (безвреден, но
      чужой), цели еды по его телу и мужская формула, промпты ночного разбора
      и Денег про Сашу, заводские ID баз Notion без хаба.
- [x] Деньги 1.8 (25.09): наличные ↔ карта — банкомат в пуше и выписке сам
      идёт из кошелька; «внёс 200 000 на Тинькофф из наличных» голосом
      списывает кошелёк сразу и склеивается с пополнением, когда приедет пуш.
      Сбор образцов денежных уведомлений для разборщиков новых банков.
- [x] Переразбор истории (25.09): правка разбора приходит с шагом, который в
      первый запуск сборки выводит накопленное заново из сырья
      (`core/HistoryFixes.kt`); первый шаг — пуши Т-Банка с местом второй
      строкой (внесение через банкомат).
- [x] Общие Деньги (25.09): журнал событий на каждом телефоне, обмен через
      семейный Google Drive (вход браузером, без аккаунта в телефоне, доступ
      только к своим файлам), слияние поле к полю — последняя правка, категория
      человека сильнее справочника, записи не удаляются; строка обмена во
      вкладке, «синхронизировать сейчас».
- [x] Копия базы в Drive (25.09): ночной архив каждого телефона уезжает в
      `Правка/Копии базы` семейного Drive по Wi-Fi; там неделя и по месяцу.
- [x] Кнопка гарнитуры (25.09): «помощник» гарнитуры (у Shokz OpenComm2
      2025 — Mute две секунды вне звонка) заводит тейк — курсор в поле:
      Правка в поле, поля нет или экран заблокирован: Засечка; слушает
      гарнитура («чем нажал — тем и слушаем»), нажатие посреди тейка и
      закрытое гарнитурой распознавание его заканчивают. Решение —
      `core/HeadsetPress.kt` с тестами, стек — `provider/HeadsetVoice.kt`,
      в «Микрофоне и распознавании» — кто отвечает на кнопку. На телефоне ещё
      не проверено: как ведут себя Samsung и прошивка Shokz, скажет журнал.
      26.09 на телефоне: «не реагирует, хотя раньше запускал gemini» — выбор
      «чем открыть» с замка не показывается; добавлены «Назначить Правку» в
      настройках и строка журнала на каждое дошедшее нажатие.
- [x] Развилка Засечки (25.09): «запиши мысль», «я тут подумал» — коммент к
      текущему делу; «запиши еду» — Едой сразу в дневник; «запиши дело»,
      «не забыть», «напомни» — Делами сразу в Todoist. Слова-команды —
      `core/ZasechkaIntent.kt` с тестами; итог с гарнитуры — голосом
      («записал коммент / еду / дело / дела», `provider/Speaker.kt`).
      26.09: решает Сонет (дорога «Развилка», low, около секунды), слова —
      запас без сети; «он вообще не понимает, когда писать в дела, когда в
      мысли».
- [x] Пилюля диктовки (26.09): бегущая строка всех кнопок — одна пилюля в
      одежде Gemini, на пружине, кнопки не накрывает. С завода — сверху, из-под
      строки состояния («пускай сверху вылезает!»); «Снизу» (над клавиатурой)
      и «У кнопки» — выбором в «Кнопках на экране», там же плотность стекла.
      Для «Снизу» на телефоне не проверено, видит ли метрика окна клавиатуру
      на Samsung, — скажет журнал («пилюля: над клавиатурой N dp»).
- [ ] Пуши Альфы и МКБ на телефоне Марианны — по образцам.
- [ ] База в облаке (этап 2): синхронизация папки базы.
