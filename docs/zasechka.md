# Засечка: таймшит всей жизни

> Часть спецификации Правки. Была разделом README; правится вместе с кодом режима. Карта репозитория — `CLAUDE.md`, короткий обзор — `README.md`.

## Файлы режима

Служба доступности и кнопки описаны в `CLAUDE.md` (раздел «Движок»); здесь — режимные куски: `trigger/ServiceZasechka.kt` (тап «З», плашка, помидоры, напоминания) и разбор фразы `provider/ClaudeZasechka.kt`.

- `NfcTagsSection.kt`
- `ZasechkaTab.kt`
- `core/ZasechkaEngine.kt`
- `data/IcuSweeper.kt`
- `data/NfcTag.kt`
- `data/PhoneStore.kt`
- `data/PhoneSweeper.kt`
- `data/ZasechkaCorrections.kt`
- `data/ZasechkaStore.kt`
- `data/ZasechkaSync.kt`
- `provider/ClaudeZasechka.kt`
- `trigger/AutoPilot.kt`
- `trigger/AutoPilotActivity.kt`
- `trigger/NfcTapActivity.kt`
- `trigger/ServiceZasechka.kt`
- `trigger/ZasechkaButtonController.kt`
- `trigger/ZasechkaQuickActivity.kt`
- `docs/zasechka-sheets.md`

---

## Режим 2. Засечка (таймшит)

**Кнопка «З»**: тап → сказал, чем занят → тап. Сонет
(`claude-sonnet-5`) раскладывает фразу в запись с названием, категорией и
клиентом. Конец предыдущего дела = начало следующего, поэтому лента
непрерывна. Тап по самой плашке во время диктовки убивает микрофон и
превращает её в поле для набора текстом — для конфиденциальных записей.

Одна фраза может стать **двумя** записями. Маркеры «и параллельно», «в это
время», «в то же время», «одновременно», «заодно», «фоном», «при этом», а
также «и слушал…», «и разговаривал по телефону»: первое дело идёт в основную
ленту, второе — в параллельный трек. «Готовил еду детям и параллельно смотрел
ютуб» = «Приготовление еды детям» [Еда] и ∥ «YouTube» [Потери], и еда
остаётся едой. Фраза целиком про фон («параллельно слушаю Акунина») кладёт
только параллель и текущее дело не закрывает.

Параллель можно приписать и к **уже записанному** делу: «ой, пока был в
туалете, смотрел ютуб», «во время обеда говорил с мамой». Разборщик находит
это дело в сегодняшнем списке, и границы параллели берутся у него —
придумывать время не надо. Если дело ещё идёт, параллель тоже открыта и
начинается с его начала. Само дело при этом не трогается: «пока был в X,
делал Y» — это не новая запись и не вставка X.

### Два трека

Владелец делает много дел одновременно, и лента держит это двумя слоями:

- **Основной трек** — то, чем он занят. Непрерывный, без пересечений, ровно
  24 часа. Все железные правила ниже — про него.
- **Параллельный трек** — то, что шло **поверх**: книга за рулём, YouTube за
  готовкой, звонок посреди работы, разговор с Марианной, пока варится суп.
  Пересекаться ему можно с чем угодно, **в сутки он не складывается и ни у
  кого ничего не отнимает**. В ленте он правее, со знаком «∥».

В ленте параллель свёрнута **по одному источнику на дело**: все звонки одной
строкой, весь ютуб одной, весь Клод одной — «Звонки ×3 · Мама, Петя · 18 м».
Иначе час готовки с ютубом и тремя звонками давал бы пять строк мелочи. Тап по
свёртке разворачивает её в отдельные заходы, тап по единственному заходу —
сразу правка.

Именно поэтому врезки телефона снова включены: они больше не режут дело.
Час за рулём с Акуниным честно лучше часа просто за рулём, поэтому **баллы
второго трека идут в балл дня целиком** — но отдельным числом: «Баланс +7 =
+30 и −23, и ещё +6 параллельно». Открытая параллель может быть только одна:
новая закрывает предыдущую, «перерыв/конец дня» и засыпание закрывают обе, а
закрыть только её можно её же ✕.

### Железные правила ленты (аудиторские)

1. **Основная лента никогда не пересекается, день складывается ровно в
   24 часа.** Параллельный трек в этот счёт не входит.
2. **Человек сильнее робота**: ручная запись всегда побеждает автоматику,
   но не стирает её.
3. **Неучтённого времени не бывает** — дыры зарастают категорией
   «Не размечено» (дыры считаются только по основному треку).
4. **Ничего не вычитается.** Ютуб и звонки не режут дело: если время уже
   занято, они ложатся параллельным треком поверх него.

Полночь режет дело на вчерашний хвост и сегодняшнюю голову. При загрузке
лента чинит старые наложения тем же расщеплением.

### Как выглядит день

- Сверху — дата, под ней **балл дня радужной полоской**: дорожка от нуля
  вправо через синий, зелёный и жёлтый к красному (сотня очков за день), у
  нуля заливка срезана ровно, скругление только на дальнем конце. Кружок с
  числом едет по дорожке и стоит в своей точке, цвет — свой из радуги.
- Дело, порезанное автоматикой, — **один блок**: высокая линия на весь период,
  рядом чистое Σ-время, прерывания параллельными строками внутри (без
  диапазона времени, выровнены по колонкам родителя). ✎ и ✕ блока действуют
  на все куски сразу.
- Каждая строка показывает **свои очки** (часы × ценность категории) той же
  радугой: ноль и −1 зелёные, +9 жёлтый, +20 красный.
- Итоги: **все** категории в радужном порядке (красная работа сверху, потери
  внизу) с временем и очками, а над ними разбор «Баланс +7 = +30 и −23».
  Цель владельца — перевёрнутый треугольник.

### Категории

Список правится в Настройках Засечки; у каждой категории есть подсказка для
Сонета (что сюда относится) и два числа:

- **Базовое время** — сколько такое дело обычно длится. Пережившее его дело
  заставляет кнопку моргнуть и спросить «Всё ещё «Обед»?»: «Да» — считаем
  дальше, «Нет» — сразу новая запись. Тумблер выключает вопросы целиком.
- **Ценность часа** от −10 до +10: сервисное около нуля, работа и спорт вверх,
  отдых и потери вниз. Из неё складывается балл дня.

Текущая цена: работа привлечение/текущая +10, планирование +9, звонки +7,
спорт +8…+9, систематизация +4, семья +6, чтение +6, еда/быт +1, сон 0,
отдых −2, **потери −5**. Цвет категории — её место в радуге эффективности.

**«Потери»** — категория-автомат. Пять минут тишины после конца последнего
дела — и в ленте открыто тикают живые потери. Закрытая дыра ≥5 минут
становится записью «потери» после 45-минутного карантина (окно для
ретро-диктовки); авто-YouTube падает туда же. Настоящая запись поверх потерь
вычитается из них мгновенно — потери обнимают дело с двух сторон, крошки
короче 5 минут исчезают. Удалять потери бесполезно по замыслу: неназванное
время отрастает обратно, выход один — назвать его делом.

### Самообучение

**Каждую ночь** (3–5 утра, раз в сутки) Опус сверяет пары «что владелец
надиктовал → что из этого вышло в ленте»: время, категория, название,
параллельность, клиент. Материал — вся история надиктовок, а не только то,
что владелец правил руками, и это принципиально: **самый частый промах он не
правит, он к нему привыкает**. Сказал «с 18:30 до 18:50», а записалось только
начало — дело растянулось на полдня, и никто не вмешался. Из пары «фраза →
запись» это видно, из правок — нет.

Ручные правки идут дополнительным, более сильным сигналом: там владелец
сказал прямо. Крючок висит на `update` — единственной точке, через которую
проходят все правки: руками, цепочкой и голосом.

Ночной разбор уходит **батчем** (`POST /v1/messages/batches`): это Опус на
сотне пар, а батч стоит ровно половину. Ночью ответа никто не ждёт, так что
единственная цена батча — он отвечает в течение часа, а обещает сутки — здесь
не цена вовсе. Заявка уходит в окно 3–5 утра, ответ забирается на любом
последующем тике одним дешёвым GET; водяной знак двигается только когда ответ
забран, так что не дошедший батч не съедает материал. Кнопки в настройках
считают обычным вызовом и по полной цене: там владелец стоит над экраном.

Нашлась закономерность — над кнопкой «З» загорается **⭐**, тап ведёт в
Засечку. **Правило заработает только после «да»**: иначе робот однажды начал
бы учить сам себя на своих же промахах. В настройках есть и ручной прогон, и
«Прогнать всю историю» — для первого раза, когда в ленте лежат месяцы
надиктовок, которых разбор не видел.

Одобренные правила едут **в конец стабильной части промпта**, к остальному
своду, а не в живой хвост рядом с фразой: читаются как правила, а не как ещё
одно поле, и лежат под часовым кэшем — меняются раз в несколько дней, а фраза
приходит десятки раз в день. Механика взята у Правки целиком (`RulesStore` с
отдельным файлом и флагом `pending`), но предмет другой: не как владелец
пишет, а что у него значат слова про время.

### Правка ленты

Голосом («поменяй…», «удали…», «еда была с 16:43 до 17:40») и руками (✎).
Правка и удаление действуют на всё разрезанное дело целиком. Любую свою
операцию можно откатить: «↩︎ Отменить …» рядом с переключателем День/Неделя
и в меню долгого нажатия «З» — лента возвращается ровно в то состояние, что
было до операции (пять шагов в памяти).

При разборе фразы Сонет видит сегодняшнюю ленту **и словарь дел за четыре
прошлых дня** (собственные названия владельца, включая поправленные руками),
поэтому одно и то же дело не приезжает каждый день под новым именем.

### Автоматика

- **Телефонный слой** (статистика использования + журнал звонков): экранное
  время, минуты по приложениям, подъёмы, «отвлечения» (взял-глянул-убрал
  быстрее 2 минут). В ленту попадают два исключения — «пожиратели внимания»
  (YouTube и кто назначен тапом по приложению) и звонки ≥1 минуты. Ни тот, ни
  другой ничего не режут: **если это время владелец уже занял делом (хотя бы
  наполовину), факт ложится параллельным треком поверх него**; если время
  было ничьё — обычной строкой, и тогда он честно закрывает неразмеченное.
  То, что владелец сам назвал тем же самым («смотрю ютуб» плюс ютуб из
  статистики), не задваивается. Тумблер — «Звонки и пожиратели —
  параллельным треком», включён.
- **Список «Приложения в ленте»** — тумблерами, а не догадками по серому
  списку экранного времени. Выключенное остаётся в списке со своей
  категорией: тумблер обратно — и оно снова пишется. Тап по строке —
  категория и «звук в фоне», ✕ — убрать совсем. По умолчанию заведены
  YouTube (Потери), Слушалка (Чтение, звук в фоне), Телеграм (Социальное:
  внешнее) и Клод (Систематизация) — категории тут предположение, меняются
  одним тапом.
- **Живая параллель.** Сессия попадала в ленту только КОГДА КОНЧАЛАСЬ, плюс
  до пяти минут на тик службы: владелец сидел в Клоде и не видел этого в
  ленте вовсе. Теперь идущая сессия (и играющая книга) открывает параллель
  сразу, как только переживёт порог, а закрывает её тот же свип, который
  увидит конец. Чужую открытую параллель — сказанную голосом — автоматика не
  трогает: слово владельца сильнее.
- **Параллель не тикает дольше своего дела** (`capOpenParallelLocked`).
  Владелец нашёл это прямо на экране: «параллельное дело должно заканчиваться
  вместе с основным, этот счётчик не может идти больше конца основного дела».
  Он прав и по смыслу второго трека: параллель — это «что шло ПОВЕРХ вот
  этого», а поверх кончившегося дела идти нечему; без правила счётчик уезжал
  в бесконечность — «Клод 4 ч» под делом на двадцать минут. Инвариант живёт
  в нормализации, поэтому держится, как бы дело ни кончилось: голосом, новым
  делом, кнопкой, правкой времени задним числом. Хозяин ищется ЦЕПОЧКОЙ —
  полночь и врезки режут запись, но не смысл, и параллель живёт до конца всей
  цепочки, а не до первого шва. Трогается только ОТКРЫТАЯ параллель, то есть
  сам счётчик: закрытая — это факт с измеренными границами (книга за рулём из
  фоновой службы, разметка задним числом за месяц), и обрезать её концом
  первого же дела значило бы стирать прожитое.
  Приложение при этом продолжает работать, поэтому свип открывает параллель
  заново — уже поверх нового дела, — и сессия Клода становится не одной
  висящей строкой, а строкой на дело. Начало новой сдвигается за ВСЕ уже
  записанные куски этой же сессии: шаг «до первого» топтался бы на месте и
  плодил одинаковые строки каждый тик (нашлось прогоном трёх тысяч
  смоделированных дней — там же проверено, что сумма кусков равна сессии,
  наложений нет, а расхождение не выходит за пятиминутный тик).
- **«Звук в фоне»** — второй режим приложения (тап по нему в списке).
  Аудиокниги, подкасты и музыку нельзя считать по переднему плану: книга
  играет с погашенным экраном, а сессия приложения кончается на первом же
  гашении — её время не поймать вовсе. Такие приложения считаются по
  **фоновой службе** (`FOREGROUND_SERVICE_START/STOP`, Android 10+): служба
  живёт ровно столько, сколько идёт воспроизведение. Пауза короче пяти минут
  склеивается в один сеанс, висящий больше суток хвост бросается. Передний
  план у них в ленту не идёт — две дороги дали бы дубль. Своя **Слушалка**
  (`ru.zf.slushalka`) отмечена так по умолчанию, категория «Чтение».
- **Разметка задним числом** (кнопка в телефонном слое). Врезки были
  выключены месяцами, и всё это время ютуб, звонки и Клод нигде не
  записывались — а телефон их помнит. Скан за 7/30/90 дней показывает, что
  нашлось, даёт каждому источнику категорию (не угадывает: «Клод» может быть
  и работой, и систематизацией) и кладёт выбранное **во второй трек**.
  Только туда: прошлые дни уже сложились в свои 24 часа, а нормализация
  дальше десяти суток не заглядывает — исправлять наложения в старом дне было
  бы некому. Идемпотентно: повторный проход ничего не задваивает. Глубина
  честная и видна в диалоге — журнал звонков телефон держит месяцами, а
  поимённые события статистики использования около недели (дальше остаются
  только суммы за день, без границ сессий, а значит и без места на шкале).
- **Сон** — по экрану: самый длинный ночной разрыв «погас—включился» ≥3 ч
  становится записью «Сон» и заодно закрывает вечернее дело и забытую
  параллель (спящий человек ничего не слушает). Сон и тренировки icu —
  единственная автоматика, которая по-прежнему режет ленту: это время занято
  по-настоящему.
- **intervals.icu** — тренировки (athlete id + API key в настройках Засечки):
  бег/вело/силовая/ходьба ложатся в свои категории, Garmin-длительность и
  счёт сна дописываются к записи «сон».
- **Помидоры** на кнопке «З»: долгое нажатие → 🍅 25/50 или перерыв 5; кнопка
  становится счётчиком минут, готовый помидор вибрирует, предлагает перерыв и
  записывается на текущее дело (🍅×N в ленте).
- **Напоминания**: пульс и уведомление о дырах во времени, утром «день
  начался?», вечером «закрыть день?»; раз в час кнопка подмигивает.
- Меню долгого нажатия «З» первой строкой показывает, что идёт сейчас и с
  какого времени.

Таймеры переживают обновление APK: дедлайн помидора и идущее дело лежат на
диске, служба поднимается сама, дозревший во время установки помидор
дозасчитывается.

### Автопилот (Wi-Fi, эфир, Bluetooth, движение)

`trigger/AutoPilot.kt` собирает сигналы и показывает пуши; ЧТО решить по
сигналу — `core/AutoPilotRules.kt`, покрыт JVM-тестами на случаях владельца.
Настройки → Засечка → Автопилот: строка состояния («вижу сеть …», «в эфире:
Летово», «последнее: …»), список причин, почему автопилот слеп, — каждая с
кнопкой-лечилкой (Wi-Fi выключен, местоположение не «всегда», геолокация,
уведомления запрещены или канал заглушен, нет доступа к Bluetooth).

- **Места.** Сеть → имя («дом», «Летово»). Два режима: **«подключился»** — точнее,
  так ловится дом (колбэк ConnectivityManager с именем сети); **«вижу сеть»** —
  для Летово, к чьему Wi-Fi не подключаются: приезд, как только сеть появилась
  в эфире уверенно (≥ −75 dBm). Скан эфира — свой раз в полчаса (больше фону
  не дают) **плюс подписка на сканы самой системы**; результаты старше десяти
  минут — кэш, а не эфир, по ним ничего не решается. Слабый сигнал — «ещё
  здесь», но не «приехал». Уехал из места «по видимости» = пяти минут свежих
  сканов без этой сети.
- **Приезд** (`AutoPilotRules.arrival`). Открыта дорога (категория
  «Передвижение…» или слово «поездка/еду/такси…» в названии) — закрывается сама,
  пуш «✓ Приехал: дом — «Поездка» закрыта, 35 мин. Что теперь?» с кнопкой
  «Сказать» (тот же путь, что тап по «З»). Открыта тренировка — не закрываем,
  предлагаем. Открыто обычное дело — спрашиваем **только если доказано
  перемещение**: зафиксирован отъезд (сеть прошлого места пропала) ПОЗЖЕ начала
  дела, и место сменилось или сети не было дольше получаса. Тогда «В ленте всё
  ещё «Встреча с Серёжей» с 14:00 — сеть «Летово» пропала в 15:30» с кнопками
  «Ехал с 15:30» (закрывает встречу там, пишет дорогу до сейчас) и «Сказать».
  Ничего не открыто и место сменилось — «что делаешь?» с теми же кнопками.
  Мигание роутера при открытой «Работе» и перезапуск службы (отъезда не было) —
  молчание, как и раньше.
- **Отъезд.** Пропало подключение — три минуты на «роутер мигнул», потом пуш
  «Уехал из «Летово»? В ленте всё ещё «Встреча», 55 мин» с кнопками
  «Транспорт»/«Пешком»: дорога стартует с момента потери сети и закрывает дело
  там же. Если дорога уже идёт — молчим. Факт отъезда запоминается всегда, даже
  при выключенном вопросе, — им доказывается перемещение при приезде.
- **Машина.** Устройство выбирается из спаренных; в настройки едут имя **и
  адрес** — имя система отдаёт не всегда, адрес приходит с каждым событием.
  Подключилась — **«Поездка на машине» [Передвижение: транспорт] начинается
  сама** с момента подключения и закрывает текущее дело (владелец: «всегда
  переключать текущее дело на передвижение на машине»); пуш с кнопкой
  «Отменить» — она убирает поездку и возвращает прежнее дело одним шагом
  (`ZasechkaStore.revertAutoStart`). Источник записи — владельческий, как у
  кнопки «Поехали», не `auto`: иначе книга за рулём из Слушалки легла бы в
  основной трек поверх поездки. Если дорога уже идёт — не трогаем. Тумблер
  выключен — остаётся старый вопрос «сел в машину?». Отключилась — дорогу не
  закрываем (заглушил у магазина — не приезд), через две минуты без
  переподключения пуш «Приехал?» с кнопкой закрыть на момент отключения; приезд
  домой по Wi-Fi закроет её и так.
- **«Точно ещё …?»** — датчик значимого движения после получаса сидячего дела
  (работа, систематизация, чтение), не чаще раза в двадцать минут.
- **Пуши — канал IMPORTANCE_HIGH** (`pravka-auto-hi`): вопрос, требующий
  ответа, должен всплыть, а не лечь тихой строкой в шторку. Прежний канал
  DEFAULT удаляется при первом пуше. Все действия кнопок — через
  `AutoPilotActivity`, всё пишется в журнал событий словами.

### Выгрузки

- **Google Sheets** через Apps Script-вебхук — настройка в
  [docs/zasechka-sheets.md](docs/zasechka-sheets.md).
- **CSV**: `date,start,end,track,minutes_day,minutes_parallel,over,title,
  category,client,useful,source,is_open,value,points,base_min,raw`. Идущая
  запись закрывается временем «сейчас» с флагом `is_open`.

  Колонки `minutes` здесь нет **намеренно**: модель, которой скормили файл,
  складывает всё, что похоже на минуты, и с параллельным треком получила бы
  тридцатичасовые сутки. Вместо неё две — `minutes_day` (сколько это заняло в
  сутках; у параллельной строки пусто) и `minutes_parallel` (наложения).
  **Сумма любой из них — честное число, и перепутать нечем.** `over` у
  параллельной строки говорит, поверх какого дела она шла, а `id` и
  `parallel_of` связывают её с носителем НОМЕРОМ. Это по замечанию читателя
  файла, и он прав: «одинаковый старт плюс одинаковая надиктовка» — догадка,
  а не связь; он прогнал эту эвристику по всему файлу и получил семь пар, из
  которых настоящей была одна.

**CSV всей жизни переделан — и это была настоящая дыра.** Владелец скормил
модели `pravka-zhizn.csv` (`DigestBuilder.lifeCsvIntent`) и получил разнос по
делу: сутки весили 1750–1805 минут, «Укладывание детей 28 мин» и «программирую
засечку 27 мин» с одним стартом давали 55 минут там, где прошло 28, а вывод был
«старые дни переписались задним числом». Проверка по самому файлу: **лента
цела** — если отделить второй трек, основной сходится в 1440 (26–30 августа:
1435, 1439, 1441, 1439, 1440), и все до единой пересекающиеся строки оказались
авто-параллелями (звонки, Телеграм, ютуб, Клод). Врал не таймшит, врала
ВЫГРУЗКА: у «всей жизни» не было ни легенды, ни признака слоя, а домены
«тренировка», «силовая» и «зарядка» — это пометки на том же времени, которое
таймшит уже посчитал, а не время сверх него. Теперь у файла есть легенда и
четыре машинные колонки: `id`, `parallel_of`, `source` (manual/auto) и
`budget`. Складывать можно ровно одно — `minutes` при `budget=1`, — и это
сутки. Урок общий: формат, который нельзя сложить, обязан говорить об этом
первой строкой, иначе честный читатель сделает из него неверный вывод.

**Второй разбор того же файла — шесть замечаний, все по делу.** Бейзлайн
21–30.08 воспроизвёлся по `budget=1` с точностью до десятой доли часа (239,7
при норме 240), и читатель сам снял вчерашний диагноз: «кривой была не жизнь,
а экспорт». Что чинилось дальше:

- **Два округления от одного числа.** `minutes` считались округлением до
  ближайшей минуты, а `time` печаталось усечением — и «время начала плюс
  minutes» не попадало в начало следующей строки на половине стыков (у него
  107 разрывов на 291, сто ровно в минуту). Теперь время печатается той же
  округлённой минутой (`ZasechkaStore.hm`). Проверено на 19 тысячах стыков:
  при усечении расходится 51%, при округлении — ноль.
- **Щели короче минуты** (`closeHairlineGapsLocked`). Лента обещала быть
  непрерывной и ею не была: заполнитель брался только за дыры от пяти минут, а
  секунды между делами не закрывал никто — оттого сутки выходили в 1428 минут.
  Щель короче минуты — не «Не размечено», а мусор округления, и закрывается
  молча концом предыдущей записи; всё, что длиннее, по-прежнему становится
  честной строкой. На шестистах смоделированных днях: было 539 дней мимо 1440,
  стало ноль.
- **Один источник правды о длительности.** Минуты дублировались в `detail` и
  расходились с колонкой `minutes` в 43 строках из 303. Из `detail` убраны.
- **Уникальность id.** Два приёма пищи с одной секундой давали один номер —
  еда теперь нумеруется своим id, плюс общий проход, который не даст совпасть
  никаким двум.
- **Параллель внутри своего носителя** (`splitByHostsLocked`). Ютуб на 25 минут
  висел на «Смене белья» длиной 13 — параллель длиннее того, поверх чего шла.
  Теперь параллель режется по границам дел: одна строка — один носитель, ни
  минуты при этом не теряется. Вылет остался только там, где резать нельзя:
  обрезок короче полуминуты лента считает мусором и выбрасывает, поэтому
  граница ≤30 с (было до 7 минут у 5,6% строк). Заодно `hostOf` перестал
  отвечать делом, задевшим параллель краем: носителем считается тот, кто
  накрывает хотя бы половину, иначе честный ответ — «поверх неразмеченного».
- **Легенда обещала больше, чем есть.** Общая надиктовка у параллели и
  носителя бывает только у сказанной голосом (у автоматической её нет вовсе), а
  домены, по которым нет данных, в файле просто отсутствуют — силовая, снятая
  часами, приезжает доменом «тренировка».

**Вся жизнь — в Notion, раз в час, сама** (`data/NotionLifeSync.kt`). Владелец:
«чтобы засечка уходила в notion, чтобы я не выгружал csv с жизнью». Структуру
придумывать не пришлось — она уже стояла: под хабом «Правка: разборы» его
аналитик (проект в Клоде) построил базы и подписал поля «для синхронизатора».
Приложение пишет в четыре из них:

- **Засечка** — журнал событий, строка на событие всех доменов, схема один в
  один как у CSV «всей жизни»: `Домен` (таймшит / таймшит∥ / тренировка /
  силовая / зарядка / еда / комментарий), `Бюджет`, `Источник`, `EntryId`
  (`t42`, `f…`, `w…`, `s…`, `g…`, `c…` — те же ключи, что в CSV), `Носитель ID`
  и настоящая связь «Носитель» на страницу дела, `Минуты` (у основного трека —
  той же разностью минут суток), еда с КБЖУ, тренировки с км/пульсом/ваттами,
  сон с приписки Garmin. `День` — сутки владельца датой без времени: SQL-слой
  Notion отдаёт `Дату` в UTC и режет день в 03:00 по Москве (проверка 02.09:
  сутки, сложенные по `Дате`, дали 1376 и 1506 вместо 1440 — на ровном месте,
  из-за пояса), а SQL-запросы на его тарифе ещё и упираются в лимит, поэтому
  у базы есть представление «Лента · новое сверху» для чтения страницами без
  SQL. Всё это записано и на хабе в «Как читать данные (для разбора)».
  Категория стоит и у строк вне ленты (еда, тренировки по виду спорта,
  силовая, зарядка, комментарий): без неё сто с лишним строк не видел ни один
  фильтр по категориям — нашёл разбор, не я. Сутки без единой минуты покрытия
  и без единого приёма еды строкой не заводятся: день до начала ведения ленты
  давал в базе строку из одних нулей.
- **Дни** — строка на сутки: покрытие и «Полный день» (≥1370 мин по правилу
  хаба), минуты по категориям, туалет/диван/соло по названиям, экран
  параллелью (YouTube/Claude/Telegram), ночной сон цепочкой через полночь с
  «Отбой»/«Подъём», «Первое действие через», еда за день, контакты. ЕГО поля —
  «Дети дома», «Марианна дома днём», «Якорь утра», «Заметка дня» — не
  трогаются: их заполняет человек. «Работа» здесь строго по категории
  «Работа: *»; клиентские встречи из «Социального» — суждение аналитика.
- **Паттерны** — паттерны ночного поиска приложения попадают в библиотеку
  аналитика как «Кандидат» от «Приложения»; наше поле — «Заявлено
  приложением». Статус — источник правды аналитика, приложение его не
  переписывает. Сопоставление по тому же ключу, что у `AnalysisStore.Pattern`.
- **Подтверждения** — единственная база, которой не было; владелец просил
  именно это: «отдельно паттерны и подтверждения, потому что я хочу работать
  над этим». Одна строка — одна датированная улика: «Саша: подтверждаю» (его
  вердикт в приложении), «Приложение: точка» (ночной поиск увидел снова),
  «Claude: точка/контрточка/сбой» (проверка в разборе). Связь с паттерном
  двусторонняя — на карточке паттерна виден список его подтверждений.
  Идемпотентность — по полю «Ключ».

Мы гости в его базах, правила как у «Дневника»: трогаем только свои колонки,
чужие строки не удаляем. Механика: раз в час полный обход — по каждому событию
снимок полей и хеш, изменилось — в очередь; очередь разгребается на каждом
пятиминутном тике пачкой до 120 запросов с паузой 340 мс (Notion пускает три в
секунду). Первый заезд — вся история, около часа; дальше пара десятков правок в
час. Пары «ключ — страница» живут в `notion-life.json`; при пустом файле карта
один раз восстанавливается из самих баз, иначе переустановка наплодила бы
дублей.

**Одна находка — один паттерн.** Ночной поиск формулирует своими словами и не
знает библиотеки разбора: «работа появляется только там, где встречу поставил
кто-то другой» и «работа появляется только из чужого запроса» — один механизм,
но в базе две строки, и вердикт владельца стоит на обеих (нашёл разбор на
второй день жизни базы: десять двойников, у соло сразу три формулировки).
Поэтому перед заведением строки находка сверяется со ВСЕЙ библиотекой:
сначала пересечение слов по корням (дёшево, ловит явные повторы), и только на
спорных — вопрос модели «тот же это механизм или другой». Шаг сверки
**суточный и батчем**: новые формулировки приносит ночной поиск, то есть раз в
сутки, и спрашивать чаще незачем (владелец: «зачем каждый час паттерны
искать?»). Пачка уходит одним запросом Fable 5.1 за половину цены — модель
здесь самая сильная не из щедрости: слить паттерн с чужим вердиктом владельца
дороже, чем оставить два похожих, поэтому в промпте прямо сказано «сомневаешься
— отвечай -1». Решение принимается один раз и живёт в `notion-life.json`. Дубль строкой не становится — он становится
точкой в «Подтверждениях» под каноническим паттерном. Двойникам, заведённым до
появления сверки, проставляется связь «Дубль чего»: сливать чужую библиотеку
приложение не вправе, но показать, что сливать, обязано.

Хеш значит «отправлено ЦЕЛИКОМ»: если связь «Носитель» не нашла
страницу (дело ещё идёт — открытые в Notion не уезжают), хеш строке не
пишется, и следующий обход доставит связь, когда дело закроется. Без этого
параллель поверх текущего дела навсегда оставалась бы с «Носитель ID» и
пустой клеткой рядом — проверка 02.09 нашла ровно такие строки. Ответ 400
(Notion не принял ИМЕННО эту строку: неизвестная опция select, длинное поле)
пропускает строку и едет дальше: одна кривая запись не должна держать всю
жизнь владельца за дверью. Отвергнутые считаются в бюджет тика.
Лента постоянно перенумеровывает куски (полночь, врезки, склейки) —
страница исчезнувшей записи АРХИВИРУЕТСЯ, иначе база зарастёт призраками и
сутки перестанут сходиться. Базы под хабом находятся по названиям среди его
детей; хаб не открыт интеграции — берутся известные id и пишется подсказка.

**Две кнопки выгрузки вместо тумблера** — «С параллельными» и «Без», у обеих
выгрузок (Засечка и «вся жизнь»). Тумблер надо помнить, а кнопка называет то,
что отдаёт, в момент нажатия. Файлы называются по-разному
(`…-bez-parallelej.csv`): два одинаковых имени в мессенджере не различить, а
перепутать слои — потерять полдня разбора. Легенда файла без параллелей прямо
говорит, что слой исключён нарочно, а не потерян.

**Потолок длительности тренировки** (`IcuSportSync.saneSeconds`): в ленте
лежало «Вело — 10471 минута», семь суток в седле, и прогулки по 488 минут.
Это не наша арифметика, это честный `elapsed_time` часов, которые забыли
остановить. Elapsed сам по себе правдивее moving (кофе посреди покатушки —
часть покатушки), поэтому он и берётся — пока правдоподобен: не больше чем
втрое дольше времени в движении, и не длиннее двенадцати часов.

**«Ютуб» и «YouTube» — одно и то же** (`APP_ALIASES`). Владелец сказал «сижу
выбираю в Ютубе наушники» (Быт, 135 минут), а статистика привезла «YouTube»
(Потери, 3 минуты) внутрь этого же куска: проверка «он уже назвал это сам»
сравнивала строки как есть и кириллицы не видела. Теперь названия приложений
приводятся к одному написанию до сравнения.

  `minutes_day` — **разность минут суток**, а не округление собственной
  длительности строки. Разница не косметическая: округляя каждую строку
  отдельно, день из двадцати записей приезжал в сумме на 1436 или 1443 минуты
  вместо 1440. Разность соседних минут телескопируется, и день сходится ровно
  в полночь. Обе минуты берутся от дня **начала** записи — иначе дело,
  кончающееся ровно в полночь (а лента режет по ней каждое такое), давало
  строку длиной −1380 минут.

  Файл начинается с **легенды** — строки с `#`: что это за файл, что треков
  два, что значит каждая колонка и какие три суммы честные. Она для того, кому
  файл отдают, — человеку и модели («в pandas это `comment='#'`»). Импорт
  Правки такие строки пропускает; таблицы покажут их первыми ячейками.
- **Импорт CSV** — обратная дорога из любой выгрузки (см. «Данные»).

(Зеркало в Notion было и удалено по решению владельца.)

---
