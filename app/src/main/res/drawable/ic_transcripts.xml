<?xml version="1.0" encoding="utf-8"?>
<!-- Lines of text - the "Расшифровки" tab. Compose's Icon() applies the
     tint, so no android:tint here (?attr/colorControlNormal came from
     AppCompat, which is no longer on the classpath). -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#000000"
        android:pathData="M4,5 h16 v2 h-16 z M4,9 h16 v2 h-16 z M4,13 h12 v2 h-12 z M4,17 h9 v2 h-9 z" />
</vector>
