<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Всегда ночь (версия 3): системная светлая тема приложение не перекрашивает. -->
    <style name="Theme.Pravka" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/pravka_window_bg</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/pravka_window_bg</item>
        <item name="android:navigationBarColor">@color/pravka_window_bg</item>
    </style>

    <style name="Theme.Transparent" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
