# Правка

Личное Android-приложение одного владельца (без Google Play): голос →
причёсанный текст прямо в поле, и на том же движке — таймшит всей жизни.
Целевое устройство: **Google Pixel 10 Pro Fold, Android 16**.
Стек: **Kotlin, Jetpack Compose (Material 3), Gradle Kotlin DSL**,
minSdk 26 / compileSdk 36. Версия **2.0**.

В репозитории живут **два приложения**: Правка (`:app`) и Слушалка
(`:slushalka`, плеер аудиокниг — своя глава ниже). Общего у них keystore,
счётчик версий и сборка CI; кода общего нет.

Внутри Правки четыре режима:

| Режим | Что делает | Точка входа |
|---|---|---|
| **Правка** | Диктовка и причёсывание текста в том поле, где он набран | Плавающая кнопка «П» |
| **Засечка** | Голосовой таймшит: лента дня, категории, балл дня | Плавающая кнопка «З», вкладка «Засечка» |
| **Разноска** | Наговор → дела в Todoist: Опус делит его на задачи, подбирает проекты, метки, сроки и приоритеты | Плавающая кнопка «Р», вкладка «Дела» |
| **Дела** | Задачи Todoist; тап по делу запускает его в ленте | Вкладка «Дела» |

Вкладки приложения: **Засечка · Дела · Настройки · Словарь · Промпты ·
Расшифровки · Обучение · Логи · Статистика**. Приложение открывается на
Засечке — это ежедневный экран, остальное обслуживает Правку.

Всё работает напрямую с API с телефона: ключ Anthropic вводится в настройках
и с устройства не уезжает. Прокси на VPS (раздел 10 ТЗ) сознательно не
делается — согласовано с владельцем.

---

## Режим 1. Правка

**Кнопка «П»** — плавающая, видна всегда, таскается пальцем, позиция
запоминается отдельно для сложенного и разложенного экрана. Она больше **не**
следит за текстовыми полями, и служба вообще не подписана на оконные события:
подглядывание за чужими окнами вызывало черноту при складывании Fold.

- **Тап** — диктовка. Запись идёт в Google Speech (потоково) или Whisper;
  распознанное вставляется в поле под курсором. Голосовые команды
  форматирования («с новой строки», «новый абзац») применяются на устройстве
  до вставки — мгновенно и бесплатно.
- **Долгое нажатие** — меню в два ряда:
  - красные, правка текста: **Причесать** (CLEAN), **Короче**, **Длиннее**,
    **Отшлифовать**, **Отменить**, **Обучить**, **Сброс**;
  - оранжевые, ассистент: **Коротко**, **Ответить**, **Перевод** — работают по
    выделению, иначе по всему полю, иначе по буферу; ответ кладётся в буфер.
- **Выделение в поле сужает любое действие** до выделенного куска.
- Результат правки **стримится прямо в текстовое поле** (не в плашку), а если
  поля под курсором нет — уезжает в буфер обмена с уведомлением.
- Другие входы: плитка в шторке и системное «Обработать текст» — обе открывают
  тот же выбор режима поверх чужого приложения.

**Режимы правки:** `CLEAN` (причесать), `BUSINESS` (деловой тон),
`SOFTEN` (смягчить). Тексты промптов лежат константами в `core/Prompts.kt`, а
их правки владельца — в DataStore (вкладка «Промпты»), поэтому обновление APK
не затирает отредактированный промпт.

**Словарь** (вкладка «Словарь») — три вида записей:
`HARD` (заменить всегда, до модели), `HINT` (подсказка модели в блоке `{DICT}`),
`PROTECT` (не трогать это слово). У каждой записи считаются срабатывания.

**Обучение** (вкладка «Обучение») — цикл «как владелец правит нас»:
служба замечает его ручные правки нашего вывода, копит их, и одним вызовом
Opus превращает пачку в предложения — в словарь или в правила. Одобренные
**правила** (`RulesStore`) едут в каждый CLEAN-запрос постоянным блоком: так
модель «помнит» систематические предпочтения. Тумблер
«Автоподхват правок» по умолчанию **выключен** (владелец: правила уже набраны,
а слежка за полями стоила плавности при складывании).

**Расшифровки** — журнал распознаваний с метриками (движок, длина аудио,
время, символы). **Статистика** — расход токенов и денег по дням.
**Эвалы** (`core/EvalRunner.kt`) прогоняют золотой набор через текущие
промпт/словарь/правила: изменения промпта измеряются, а не «на глаз».

---

## Режим 2. Засечка (таймшит)

**Кнопка «З»**: тап → сказал, чем занят → тап. Сонет
(`claude-sonnet-5`) раскладывает фразу в запись с названием, категорией и
клиентом. Конец предыдущего дела = начало следующего, поэтому лента
непрерывна. Тап по самой плашке во время диктовки убивает микрофон и
превращает её в поле для набора текстом — для конфиденциальных записей.

### Железные правила ленты (аудиторские)

1. **Лента никогда не пересекается, день складывается ровно в 24 часа.**
2. **Человек сильнее робота**: ручная запись всегда побеждает автоматику,
   но не стирает её — авто-факт становится прерыванием внутри дела.
3. **Неучтённого времени не бывает** — дыры зарастают категорией «Потери».

Ретро-заявление («обедаю с 16:43», сказано в 16:53) режется вокруг уже
записанных авто-фактов: YouTube 16:43–16:50 остаётся прерыванием, обед
показывается блоком, чистое время еды считается за вычетом. Полночь режет
дело на вчерашний хвост и сегодняшнюю голову. При загрузке лента чинит старые
наложения тем же расщеплением.

### Как выглядит день

- Сверху — дата, под ней **балл дня радужной полоской**: дорожка от нуля
  вправо через синий, зелёный и жёлтый к красному (сотня очков за день), у
  нуля заливка срезана ровно, скругление только на дальнем конце. Кружок с
  числом едет по дорожке и стоит в своей точке, цвет — свой из радуги.
- Дело, порезанное автоматикой, — **один блок**: высокая линия на весь период,
  рядом чистое Σ-время, прерывания параллельными строками внутри (без
  диапазона времени, выровнены по колонкам родителя). ✎ и ✕ блока действуют
  на все куски сразу.
- Каждая строка показывает **свои очки** (часы × ценность категории) той же
  радугой: ноль и −1 зелёные, +9 жёлтый, +20 красный.
- Итоги: **все** категории в радужном порядке (красная работа сверху, потери
  внизу) с временем и очками, а над ними разбор «Баланс +7 = +30 и −23».
  Цель владельца — перевёрнутый треугольник.

### Категории

Список правится в Настройках Засечки; у каждой категории есть подсказка для
Сонета (что сюда относится) и два числа:

- **Базовое время** — сколько такое дело обычно длится. Пережившее его дело
  заставляет кнопку моргнуть и спросить «Всё ещё «Обед»?»: «Да» — считаем
  дальше, «Нет» — сразу новая запись. Тумблер выключает вопросы целиком.
- **Ценность часа** от −10 до +10: сервисное около нуля, работа и спорт вверх,
  отдых и потери вниз. Из неё складывается балл дня.

Текущая цена: работа привлечение/текущая +10, планирование +9, звонки +7,
спорт +8…+9, систематизация +4, семья +6, чтение +6, еда/быт +1, сон 0,
отдых −2, **потери −5**. Цвет категории — её место в радуге эффективности.

**«Потери»** — категория-автомат. Пять минут тишины после конца последнего
дела — и в ленте открыто тикают живые потери. Закрытая дыра ≥5 минут
становится записью «потери» после 45-минутного карантина (окно для
ретро-диктовки); авто-YouTube падает туда же. Настоящая запись поверх потерь
вычитается из них мгновенно — потери обнимают дело с двух сторон, крошки
короче 5 минут исчезают. Удалять потери бесполезно по замыслу: неназванное
время отрастает обратно, выход один — назвать его делом.

### Правка ленты

Голосом («поменяй…», «удали…», «еда была с 16:43 до 17:40») и руками (✎).
Правка и удаление действуют на всё разрезанное дело целиком. Любую свою
операцию можно откатить: «↩︎ Отменить …» рядом с переключателем День/Неделя
и в меню долгого нажатия «З» — лента возвращается ровно в то состояние, что
было до операции (пять шагов в памяти).

При разборе фразы Сонет видит сегодняшнюю ленту **и словарь дел за четыре
прошлых дня** (собственные названия владельца, включая поправленные руками),
поэтому одно и то же дело не приезжает каждый день под новым именем.

### Автоматика

- **Телефонный слой** (статистика использования + журнал звонков): экранное
  время, минуты по приложениям, подъёмы, «отвлечения» (взял-глянул-убрал
  быстрее 2 минут). В ленту попадают два исключения — «пожиратели внимания»
  (YouTube и кто назначен тапом по приложению) и звонки ≥1 минуты. Оба режут
  текущее дело, и оно продолжается после них; без активного дела пожиратель
  ложится своей строкой.
- **Сон** — по экрану: самый длинный ночной разрыв «погас—включился» ≥3 ч
  становится записью «Сон» и заодно закрывает вечернее дело.
- **intervals.icu** — тренировки (athlete id + API key в настройках Засечки):
  бег/вело/силовая/ходьба ложатся в свои категории, Garmin-длительность и
  счёт сна дописываются к записи «сон».
- **Помидоры** на кнопке «З»: долгое нажатие → 🍅 25/50 или перерыв 5; кнопка
  становится счётчиком минут, готовый помидор вибрирует, предлагает перерыв и
  записывается на текущее дело (🍅×N в ленте).
- **Напоминания**: пульс и уведомление о дырах во времени, утром «день
  начался?», вечером «закрыть день?»; раз в час кнопка подмигивает.
- Меню долгого нажатия «З» первой строкой показывает, что идёт сейчас и с
  какого времени.

Таймеры переживают обновление APK: дедлайн помидора и идущее дело лежат на
диске, служба поднимается сама, дозревший во время установки помидор
дозасчитывается.

### Выгрузки

- **Google Sheets** через Apps Script-вебхук — настройка в
  [docs/zasechka-sheets.md](docs/zasechka-sheets.md).
- **CSV**: `date,start,end,minutes,title,category,client,useful,source,is_open,
  value,points,base_min,raw`. Идущая запись закрывается временем «сейчас» с
  флагом `is_open`.
- **Импорт CSV** — обратная дорога из любой выгрузки (см. «Данные»).

(Зеркало в Notion было и удалено по решению владельца.)

---

## Режим 3. Дела (Todoist)

Доступ **прямой, личным API-токеном** (Todoist → Настройки → Интеграции →
Разработчик), без OAuth и посредников: токен живёт только на телефоне.
Приложение само выясняет, какое поколение API отвечает на аккаунте — новый
`/api/v1` (страницы по курсору) или прежний `/rest/v2` (массив).

- Группы: **«Сегодня»** (вместе с просроченным) раскрыта, **«Без даты и без
  проекта»** (входящие) и **проекты** свёрнуты, у свёрнутой видно число дел.
  Сверху поиск по вхождению слова, приоритет — цветной точкой.
- **Тап по делу** заводит текущую запись ленты. Название берётся из Todoist
  буква в букву; категория — из собственной истории этого дела, а для нового
  дела её подбирает Сонет. Правки/удаления в этом пути не бывает.
- Когда запись закрылась, в задачу уезжает **коммент со временем**:
  «⏱ 22 августа, 19:30–20:42 · 1 ч 12 м» (и число подходов, если их было
  несколько). Дописывает пятиминутный тик службы и открытие вкладки; связка
  дело→запись живёт на диске и умирает молча, если старше 36 часов.
- Список кэшируется на диск: вкладка открывается мгновенно и работает без сети.

Добавление дел **в** Todoist — это Разноска, следующий раздел.

---

## Режим 4. Разноска (наговор → дела)

**Кнопка «Р»**, синие чернила — третья в связке под «П» и «З», те же жесты и
та же плашка-тикер. Тап → наговариваешь дела подряд, как думаешь («Наташе
сверку с налоговой до пятницы, Толкушкину список фондов, и не забыть про
оплату Стеллара») → тап. Дальше **Опус** (`claude-opus-5`) режет наговор на
отдельные задачи: одна задача = одно действие.

Что модель делает с каждым делом:

- **формулировка** — «Кто: глагол + действие», как в операционке владельца;
  мяч у него самого — просто действие без префикса;
- **проект** — из живого каталога Todoist, подпроект точнее родителя
  («Стеллар Групп / buy-side M&A»); не уверена — оставляет пустым;
- **метки** — только те, что в Todoist правда есть (`жду`, `обещал`,
  `быстр`, имена команды…), выдуманные не создаются;
- **приоритет** — P1 = деньги, P2 = важно, P4 по умолчанию;
- **срок** — «в пятницу», «через две недели» превращаются в точную дату,
  повторяющееся («каждый вторник») уезжает словами в `due_string`;
- **не дела** — факты, статусы, мысли вслух собираются отдельным полем
  «заметки»: их место в CRM, а не в списке задач.

Промпт разбора правится во вкладке «Промпты» («Разноска (дела)»), там же его
можно вернуть к заводскому. В промпт уезжает и словарь Правки: имена и
термины приходят в дела в правильном написании.

### Плашка: отметить и добавить

Как только наговор разобран, рядом с кнопкой появляется **плашка** — синяя
пилюля того же покроя, что тикер «П» и плашка «З»: тот же радиус, тот же
бумажный текст, та же сторона экрана. Каждое дело строкой с проектом, метками,
сроком и приоритетом.

Кружок слева — это **отметка, а не отправка**: все дела отмечены сразу, тапом
по крупному кружку снимаешь лишние, и одно **«ОК · N»** в конце добавляет
отмеченные. Так решение принимается глазами по всему списку, а не по одному
делу с ожиданием после каждого; до «ОК» в Todoist не уходит ничего.

- **«✎»** справа — поправить формулировку на месте, в том же поле ввода, что
  у Засечки; пустой текст = вычеркнуть дело;
- **тап по строке** — дело целиком во вкладке «Дела»: текст, описание, проект,
  метки, срок, повтор, приоритет, «убрать это дело»;
- **«✕»** — убрать плашку: неотмеченные и неотправленные дела ждут во вкладке,
  ничего не теряется.

Плашка сама убирается через 45 секунд — тоже без потери разбора. После
отправки на её месте остаётся записка «✓ 3 дела в Todoist · ↩︎».

**Дубли.** Перед показом каждое дело сверяется с уже открытыми задачами
Todoist — по смыслу, а не буквально (стеммингом и пересечением слов, локально,
без токенов). Похожее помечается «⚠ похоже: …», а решение остаётся за
владельцем: железное правило «не создавать дубль» напоминает о себе, но не
запрещает.

**Долгое нажатие «Р»:** отменить отправку · отправить всё, что ждёт · показать
разбор · разобрать заново (тот же наговор, если модель ошиблась) · разобрать
текст · набрать текстом · открыть «Дела».

### Не только голос

Тот же разбор работает по чужому тексту — дайджест из чата, расшифровка
встречи, письмо:

- **«Разобрать текст»** в меню «Р» — берёт выделенное, иначе поле под
  курсором, иначе буфер обмена;
- **«Разноска»** в системном меню выделения — рядом с «Правкой»: выделил стену
  текста в любом приложении и получил из неё дела.

Стена режется по 60 тысячам знаков (об этом говорит тост) — один разбор не
должен стоить как день работы.

### Маршруты: учится на поправках

Переложил дело в другой проект, повесил другую метку, поднял приоритет —
приложение это запомнило: ««Арина: FX-модель Доставки морем» → #Доставка морем
/ валютная позиция @арина». Двадцать свежих поправок уезжают в следующий
разбор примерами, поэтому одну и ту же ошибку Разноска делает один раз, а не
каждую неделю. Тот же цикл, что в «Обучении» у Правки, только учится не
формулировкам, а раскладке. Список — во вкладке «Дела» → «Маршруты», лишнее
удаляется крестиком.

### Отмена отправки

Минуту-другую после «ОК» (окно — 10 минут) работает **«↩︎»** на записке и
«↩︎ Отменить отправку» в меню «Р»: созданные задачи удаляются из Todoist по
сохранённым id, дела снова ждут во вкладке, и плашка возвращается — поправить
и отправить заново. Дальше окна не отменяем: задачу к тому времени могли уже
начать.

### Что защищено

- Разобранный наговор лежит в `raznoska.json` **до** того, как Todoist его
  принял: нет сети, нет токена, приложение убито посреди отправки — дела
  дождутся во вкладке.
- У каждого дела своя отметка «уже создано», а запрос уезжает с постоянным
  `X-Request-Id`: повторное «Отправить» после таймаута не создаёт дублей.
- Микрофон один на три кнопки: пока идёт чужая запись, «Р» об этом говорит, а
  не перехватывает.
- Расход Опуса идёт в те же счётчики, что и всё остальное (вкладка
  «Статистика»), а стоимость разбора видна на самой карточке.

---

# Слушалка — отдельное приложение

`ru.zf.slushalka`, свой APK, свой значок, свои данные. Живёт в этом же
репозитории (модуль `:slushalka`), потому что делит с Правкой keystore,
счётчик версий и одну сборку CI. Внутри Правки её нет и быть не должно:
это плеер аудиокниг, а не режим диктовки.

Зачем: книги куплены на сайте автора и лежат в папке — в продаже их нет.
Обычные плееры про такую полку ничего не знают, а главное — теряют место
в двадцатичасовой записи, и искать его потом мучительно.

## Откуда книги

Папка на телефоне, выбранная системным пикером (SAF). Приложение читает её
как есть и ничего никуда не копирует. Разрешение сохраняется навсегда,
переспрашивать не нужно.

Раскладка простая: **папка с аудиофайлами внутри — это книга**. Рядом с
аудио кладутся обложка (любая картинка; `cover`, `folder`, `front` в имени
выигрывают) и текст книги — **fb2** или **epub**. Раскладку «диск 1 / диск 2»
слушалка собирает в одну книгу, а не в три: если в папке самой аудио нет,
но лежит текст или подпапки называются частями, куски склеиваются.

Порядок файлов — «человеческий»: «Глава 2» идёт перед «Глава 10», а не
после. Длительности меряются один раз при первом открытии книги и ложатся
в кэш: без них не посчитать, где мы в книге.

## Плеер

- **Обложка** — из файла рядом, из fb2/epub (там она обычно вшита) или из
  тега первого mp3, в этом порядке.
- **±15 секунд** (шаг настраивается: 10/15/20/30/60). Те же кнопки — в шторке
  и на экране блокировки: играет `MediaSessionService`, поэтому работают и
  кнопка гарнитуры, и автомагнитола.
- Одна строка управления: **месяц** (таймер сна, с остатком минут под ним) —
  слева, ±15 и пуск — посередине, **скорость числом** — справа. Под ней
  словами: «Главы», «Закладки», «Напомнить содержание». Ниже — «Спросить» и
  «Читать».
- Книга ставится **целиком, одним списком**: перемотка через границу файла —
  обычная перемотка, а не «открыть следующий».
- **Скорость** 0,5–3,0×, помнится по книге. **Проглатывание тишины** — тумблер.
- **Сон**: 15/30/45/60 минут или «до конца файла»; последние двадцать секунд
  звук уводится в тишину — резкий обрыв будит. «+5 минут» одним тапом.
- **Откат после паузы**: вернулся через пять минут — три секунды назад, через
  неделю — тридцать. Иначе включаешься в середину фразы.
- **Закладки** — с куском текста рядом: через месяц «2:14:30» не говорит
  ничего, а две строки текста возвращают сцену целиком.
- Разложенный Fold показывает две колонки: обложка слева, управление справа.

## Место в книге не теряется

Ровно та дисциплина, что у ленты Засечки, и по той же причине.

- Позиция пишется **тиком раз в двадцать секунд** (владелец просил
  «раз в минуту-две» — берём чаще, это дёшево), плюс на каждой паузе,
  перемотке, смене файла, сворачивании приложения и остановке службы.
  Приложение, выгруженное системой из памяти, теряет максимум двадцать секунд.
- Запись **атомарна**: tmp + `fsync` + переименование, предыдущая версия
  остаётся рядом как `.prev`, не разобравшийся файл уезжает в `.corrupt`.
- Пустая позиция поверх непустой **отклоняется** — это не «начал заново», это
  баг кода.
- **История мест**: последние сорок отметок с временем — если позиция всё-таки
  собьётся, есть куда вернуться.

### Синхронизация мест

В корне библиотеки заводится папка `_Слушалка`, и каждый пишет туда **свой**
файл `позиции-<имя>.json`. Никаких слияний: у дорожки один хозяин. Если папка
синхронизируется между устройствами — книга продолжается там, где
остановилась, а на карточке видно, докуда дошёл второй слушатель. Расхождение
больше минуты не подменяется молча, а спрашивается.

## Вопрос по книге

Кнопка «Спросить по книге» на экране плеера. Книга встаёт на паузу, вопрос
**наговаривается** (тот же системный распознаватель, что у Правки, ru-RU,
непрерывная сессия) или набирается руками — и уезжает Опусу вместе с тем
куском книги, который уже прослушан.

**Как считается место в тексте.** Складываются длительности всех файлов,
позиция в записи переводится в смещение в тексте по карте «аудио → текст».
Если число файлов совпало с числом глав, карта строится по главам — тогда
ошибка живёт внутри главы и не копится к концу книги; иначе это одна
пропорция на всю книгу. Дальше — экран «Текст книги»: тап по абзацу, который
читают прямо сейчас, ставит **ручную отметку**, и с неё привязка становится
точной. Две-три отметки — и попадание в нужное место перестаёт быть
приблизительным.

**Спойлер-барьер.** Главное в этой затее. Текст режется не по текущей секунде,
а на две минуты раньше (настраивается): привязка приблизительная, и ошибаться
она обязана в сторону уже услышанного. А в промпте отдельным правилом стоит
запрет пользоваться знанием о том, что будет дальше, — Опус знает романы
Акунина наизусть и на вопрос «кто этот человек в маске» честно ответил бы из
памяти, убив книгу. Модель отвечает так, будто дальше присланного текста
книги не существует.

- По умолчанию уезжают **последние пять страниц** (страница — 1800 знаков,
  число настраивается).
- Тумблер «вся книга до этого места» отдаёт весь текст с начала — для
  вопросов вроде «кто это?» про героя из первой главы. Кэш-блок обрывается на
  границе главы, поэтому пока слушаешь главу, он не меняется, и час кэша
  отрабатывает по-настоящему: второй вопрос за вечер стоит копейки.
- Заготовки под кнопками: «Что сейчас происходит?», «Кто здесь кто?»,
  «Что было раньше?», «Что это за словцо?» — последнее ради реалий
  позапрошлого века, за которыми Акунина и слушают.
- **Ответ вслух** — тумблер. Книга сама продолжается с того же места, когда
  ответ дочитан: за рулём в телефон уже не потянешься.
- История вопросов по книге хранится (полсотни последних) и показывается в
  том же окне. Расход считается по каждому вопросу.

## Читалка

Тот же текст, что уезжает в вопросы, можно просто читать. Кнопка «Читать» на
экране плеера — звук при этом замолкает.

- **Шрифт** — с засечками, рублёный, машинописный. **Кегль** 12–34,
  **междустрочье** 1,2–2,0, **поля** 0–48, **выключка по ширине** тумблером.
- **Бумага**: как система · чёрным по белому · сепия · серая · белым по
  чёрному (настоящий чёрный, для OLED ночью).
- **Два способа листать**, переключаются в настройках вида:
  - **прокруткой** — обычная лента, тап по краям прокручивает на экран;
  - **страницами** — настоящая разбивка: абзац меряется в той же ширине и тем
    же шрифтом, каким будет нарисован, и режется по строкам, поэтому строка
    никогда не оказывается разрезанной краем страницы пополам. Считается не вся
    книга, а окно вокруг текущего места — у романа страниц под тысячу, и мерить
    их все ради одного разворота незачем. Перелистывается смахиванием и тапом
    по краям.
- Тап посередине — показать/спрятать панели. Внизу «стр. 142 из 380», сверху
  название главы.
- Экран не гаснет, пока читаешь (тумблером).
- Место чтения пишется на диск отдельно от места слушания и тем же порядком.

### Картинки и карты

Картинки достаются из fb2 (`<binary>`) и epub и показываются **на своём месте
в тексте** — карточкой в рамке, вписанной целиком, как обложка. Под ней
подпись: из самого файла (`title`/`alt`), а если её там нет — строка, стоявшая
под картинкой в книге; такая строка тогда не повторяется отдельным абзацем.
Тап — во весь экран, двумя пальцами увеличивается: карту Москвы или план
особняка надо разглядывать, а не угадывать по миниатюре.

**На экране плеера** картинка встаёт на место обложки за страницу до того, как
о ней зайдёт речь, и уходит, когда подходит следующая: пока слушаешь про
Хитровку, перед глазами её план. Подпись — полосой понизу, как на конверте
пластинки. Нет иллюстраций рядом — на месте остаётся обложка.

Все картинки книги разом — чип «картинки» на плеере, «Показать картинки» в
настройках и «Картинки» в панели читалки.

Ссылка на картинку в fb2 бывает и `l:href`, и `xlink:href`, изредка просто
`href` — берём любой атрибут, чей хвост «href». А сами `<binary>` в части книг
лежат **перед** телом, а не после, поэтому забираются все картинки подряд, а не
только те, ссылку на которые уже встретили: обе эти мелочи по отдельности
оставляли книгу без иллюстраций.

Пока читаешь в пределах **двух страниц** от картинки, в углу висит её
миниатюра — та самая карта остаётся под рукой, когда текст на неё ссылается.

## Переключение звук ↔ текст

Место в книге одно, просто выражено в двух единицах: секундах записи и знаках
текста. Переводит одно в другое **карта книги**.

**Всё это считает сам телефон.** Ни одного запроса наружу и ни копейки: кусок
записи расшифровывает системный распознаватель Андроида (локальный,
`createOnDeviceSpeechRecognizer`; если локального нет — обычный), а поиск
расшифровки в тексте — обычный код приложения. Модель тратится ровно на две
вещи: вопросы по книге и пересказ. Больше ни на что.

Кусок отдаётся распознавателю **в 16 кГц моно** — на этой частоте работают все
распознаватели речи, и исходные 44,1 кГц они молча не принимают. Понижение
делается усреднением по окну: простое прореживание дало бы зеркальные
призвуки на согласных.

Кнопка **«Одна проба»** рядом с разметкой показывает всю цепочку разом:
сколько секунд звука вынули, что услышали дословно и нашлось ли это в книге.
Разметка тоже не отчитывается голым нулём, а говорит, где порвалось —
не декодировался звук, промолчал распознаватель или услышанное не нашлось в
тексте.

### Разметка книги

Кнопка на экране плеера (и в настройках читалки). Приложение проходит по
записи пробами — **три на каждый час**, вразнобой внутри часа, чтобы не
попадать на стыки файлов и заставки, — расшифровывает каждую и находит в
тексте. Двадцатичасовая книга — шестьдесят проб, минуты три работы.

Идёт по порядку: каждая следующая проба опирается на уже найденные точки,
поэтому окно поиска сужается с тридцати страниц до десяти, а попаданий
становится больше. Проба, которая просит поставить точку назад относительно
соседей, отбрасывается — это промах распознавания, а не открытие. Прерванную
разметку можно запустить снова: размеченные места пропускаются.

**Карта ложится файлом `слушалка-разметка.json` в папку самой книги.** Значит,
она переезжает вместе с книгой: скопировал папку на планшет или второму
слушателю — разметка уже там, считать заново не надо. В файле записано, к
какой паре «запись + текст» он сделан; другое издание или другая начитка —
карта не подойдёт и будет пропущена, а не уведёт мимо.

После разметки переход в обе стороны — **мгновенный расчёт по карте**, без
всякого распознавания.

### Пока книга не размечена

Работает сверка на лету, **в обе стороны**.

- **Звук → текст.** Читалка открывается сразу — на месте по карте, **без
  подсветки**: карта ошибается на абзац-другой, и подсвечивать догадку значило
  бы вводить в заблуждение. Дальше внизу появляется «Слушаю оригинал…»:
  вырезаются последние шесть секунд, расшифровываются и ищутся в ближайших
  абзацах. Нашлось — страница встаёт на это место и оно подсвечивается. Не
  нашлось — так и написано («не расслышал» или «услышал, но не нашёл»), место
  остаётся примерным.
- **Текст → звук.** Перемотка по карте мгновенная, но карта может ошибаться:
  приложение слушает несколько секунд с того места, куда попало, находит их в
  тексте и поправляет перемотку.

Любой такой переход оставляет на карте новую отметку — и попадает в файл
разметки рядом с книгой. Чем больше переходов, тем реже сверка нужна вообще.
Тумблером выключается.

### Как ищется расшифровка

**Не строкой**: расшифровка всегда кривовата — пропущенные слова, чужие
окончания. Ищется голосованием по парам соседних слов: каждая найденная пара
голосует за место, с которого фраза могла начаться, и правильное место
набирает голосов заметно больше остальных. Не набирает — честное «не нашёл» и
место по карте: увести читателя не туда хуже, чем промахнуться на полстраницы.

И **в окне** вокруг места с карты, а не по всей книге. Пять-семь обычных слов
встречаются в романе десятками, поиск по всей книге увёл бы в чужую главу с
полной уверенностью в себе.

Окно **расширяется ступенями**: сперва пара абзацев вокруг предполагаемого
места, потом страница, потом больше. Обычно хватает первой ступени. Сверху
окно ограничено соседними выверенными точками: между двумя отметками карты
место лежать не может, и дальше них искать незачем.

Найденное место в читалке **коротко подсвечивается и гаснет за три секунды** —
глазами сразу видно, откуда читать, а дальше ничто не мешает тексту.

Обратно — проще: в читалке место известно точно, «Слушать отсюда» переводит
его в секунды. Долгое нажатие на абзац даёт то же самое плюс отметку **«я
тут»** вручную; она тоже уезжает в файл разметки. «Сбросить отметки» убирает и
карту — вместе с файлом.

## «Что там было»

Кнопка и в плеере, и в читалке. Пересказ **последних глав** до текущего места:
одна глава, две или три — на выбор. Сонетом, вчетверо дешевле Опуса; один и
тот же пересказ второй раз не оплачивается.

Спойлеров тут не бывает по построению: дальше текущего места модели просто
нечего показали. Плюс те же правила промпта, что и у вопросов, — знанием книги
наизусть пользоваться запрещено.

## Ключи и данные Слушалки

| Что | Где |
|---|---|
| Ключ Anthropic | Настройки → Ключ Anthropic (свой, у Правки он не спрашивается) |
| Папка с книгами | Настройки → Выбрать папку |
| Имя для синхронизации | Настройки → Кто слушает |

| Файл в `filesDir` | Что это |
|---|---|
| `positions.json` | позиции, скорости, ручные отметки, история мест |
| `library.json` | разобранная библиотека (книги, файлы, длительности) |
| `bookmarks.json` | закладки |
| `asks.json` | вопросы и ответы по книгам |
| `text/*.txt`, `text/*.json` | разобранный текст книги, главы и места картинок |
| в папке книги: `слушалка-разметка.json` | карта «секунда записи ↔ знак текста» |
| `covers/*`, `images/*` | обложки и картинки, вынутые из fb2/epub и тегов |

Разрешения: микрофон (наговорить вопрос), уведомления (плеер на локскрине).
Службы доступности слушалке не нужно — она ничего не подглядывает.

---

# Правка: данные и сборка

## Данные: где лежат и как защищены

Всё в приватной памяти приложения (`filesDir`), ничего в облаке, кроме
явных зеркал.

| Файл | Что это |
|---|---|
| `zasechka.json` | лента, категории, клиенты |
| `dictionary.json` | словарь Правки |
| `pravka-rules.json` | выученные правила |
| `pravka-learn-pending.json` | предложения, ждущие одобрения |
| `pravka-edit-watch.json` | следилки за ручными правками |
| `phone.json` | телефонный слой |
| `todoist.json` | кэш дел, проектов, меток и связки с лентой |
| `raznoska.json` | разобранные наговоры Разноски, ещё не уехавшие в Todoist |
| `raznoska-routes.json` | маршруты: куда владелец сам перекладывал дела |
| `history.jsonl`, `transcriptions.jsonl`, `*.log` | журналы (сами ротируются) |

**Дисциплина записи.** Файлы пишутся атомарно: временный файл, `fsync`,
переименование поверх. Предыдущая версия остаётся рядом (`.prev`) и читается,
если основной файл пропал или не разобрался; не разобравшийся файл уезжает в
`.corrupt`, а не затирается пустым.

**Копии.** Раз в час на диск снимается копия всего незаменимого (лента,
словарь, правила, находки, следилки, телефон, дела): каждый час за последние
двое суток и последний час каждого дня за месяц. Лента вдобавок копируется
раз в день и перед любым резким сокращением записей. Список копий с кнопками
«Восстановить»/«Файлом» — в Настройках Засечки.

**Заслоны против потери дня** (после реального инцидента 22.08.2026, причиной
был `persist` во время миграции категорий — до того, как записи прочитались
из файла):

- запись пустой ленты поверх файла с записями **отклоняется** — лента не умеет
  стирать всё сразу, значит это баг кода;
- сигнализация о резком сокращении спрашивает сам файл, если счётчика в
  памяти ещё нет (первая запись после загрузки — самая опасная);
- пустая лента при загрузке автоматически поднимается из копии;
- если записей мало, а в копии втрое больше — предупреждение в журнал;
- **Импорт CSV** поднимает ленту из любой выгрузки: строки, которые уже есть
  (та же минута и то же название), не удваиваются, безымянные «потери» лента
  отращивает сама.

---

## Ключи и настройки

| Что | Где вводится |
|---|---|
| Ключ Anthropic | Настройки → API-ключ |
| Вебхук Google Sheets | Настройки Засечки |
| intervals.icu athlete id + API key | Настройки Засечки |
| Токен Todoist | Вкладка «Дела» → «Токен» |

Разрешения: служба доступности (обязательно), микрофон, показ поверх окон,
уведомления, статистика использования и журнал звонков (телефонный слой).

---

## Карта кода

```
app/src/main/java/ru/zf/pravka/
  MainActivity.kt        вкладки и навигация
  PravkaApp.kt           сервис-локатор: все сторы и движки
  ZasechkaTab.kt         вкладка Засечки (лента, итоги, настройки, копии)
  TodoistTab.kt          вкладка «Дела»
  RaznoskaSection.kt     Разноска внутри «Дел»: разбор, правка дела, отправка
  core/                  ProofreadEngine, ZasechkaEngine, RaznoskaEngine,
                         Prompts, словарь, правила, эвалы, команды, undo
  provider/              ClaudeProvider (Сонет/Опус), Whisper, Google Speech
  target/                куда писать текст: поле доступности, буфер
  trigger/               служба доступности, все три плавающие кнопки,
                         плитка, Quick Tap, «Правка» и «Разноска» в меню
                         выделения
  data/                  все сторы и синхронизации, копии, настройки
  ui/                    тема, тосты, вибро
docs/zasechka-sheets.md  как поднять зеркало в Google Sheets

slushalka/src/main/java/ru/zf/slushalka/     ОТДЕЛЬНОЕ приложение
  MainActivity.kt        экраны: библиотека, плеер, текст, настройки
  SlushalkaApp.kt        сервис-локатор
  library/               обход папки (SAF), книги, длительности, порядок файлов
  player/                PlayerHolder (плеер + тик сохранения), PlaybackService,
                         AudioChunk (кусок файла в PCM для сверки)
  text/                  fb2/epub -> текст, главы и картинки; карта «аудио -
                         текст»; поиск расшифровки в книге (Locator)
  data/                  позиции, синхронизация мест, закладки, вопросы
  ask/                   промпты и спойлер-барьер, Claude, распознавание
                         (микрофон и кусок файла), TTS
  ui/                    экраны (библиотека, плеер, читалка, вопрос,
                         пересказ), темы, обложки, кнопки плеера
```

Ключевые файлы, если надо быстро понять систему:
`data/ZasechkaStore.kt` (инварианты ленты, нормализация, копии, импорт),
`core/ZasechkaEngine.kt` (фраза → запись), `provider/ClaudeProvider.kt`
(промпты и разбор), `trigger/PravkaAccessibilityService.kt` (весь рантайм:
кнопки, диктовка, тики, напоминания).

---

## Сборка и доставка

```powershell
.\gradlew assembleDebug            # собрать оба APK: Правка и Слушалка
.\gradlew :app:assembleDebug       # только Правка
.\gradlew :slushalka:assembleDebug # только Слушалка (без NDK, собирается за минуту)
.\gradlew installDebug             # собрать и поставить по adb
.\gradlew deliverApk               # собрать и отправить APK Правки в Telegram
.\gradlew deliver                  # собрать + adb-установка + Telegram
```

APK: `app/build/outputs/apk/debug/app-debug.apk` и
`slushalka/build/outputs/apk/debug/slushalka-debug.apk`.

Счётчик сборок один на оба приложения: `:app` его увеличивает, `:slushalka`
только читает — один `assembleDebug` не должен двигать номер дважды.

**Канал 0 — GitHub Actions (основной при облачной разработке).** Каждый пуш в
любую ветку собирает подписанные debug APK и кладёт их в ветку `apk-builds`
(`pravka-debug.apk`, `slushalka-debug.apk` + `build-info.txt`). Для CI `versionCode = номер запуска
workflow`, поэтому каждая следующая сборка ставится поверх предыдущей.

**Канал 1 — adb по Wi-Fi.**

```powershell
.\scripts\reconnect.ps1 -Pair 192.168.x.x:PORT -Code 123456
.\scripts\reconnect.ps1 192.168.x.x:PORT
.\scripts\reconnect.ps1            # после перезагрузки телефона
```

**Канал 2 — Telegram-бот.** `telegram.botToken` и `telegram.chatId` в
`local.properties`, дальше `.\gradlew deliver`. Без токена таска просто пишет
«skipping Telegram delivery».

**Версии.** `version.properties` хранит счётчик `buildNumber`; каждый
`assemble*`/`install*`/`deliver*` увеличивает его. `versionCode = buildNumber`,
`versionName = 2.0.<buildNumber>`. Файл коммитится, чтобы номера не
повторялись между машинами.

### Подпись — важно

Единый keystore `keystore/pravka.jks` используется для **всех** сборок,
включая debug и включая Слушалку. Никогда не подписывать другим ключом: установка поверх
сломается, и вместе с приложением сотрутся словарь, лента и настройки.

- Пароль хранилища и ключа: `pravka-keystore-2026`
- Алиас: `pravka`
- SHA-256: `B9:E9:79:F0:2C:F2:9D:EB:35:3D:CF:2B:CE:99:C5:FC:64:E5:1A:33:27:24:CB:B6:5C:53:28:25:64:53:11:B5`

### Первая настройка на новой машине

1. JDK 17+ и Android SDK (проще через Android Studio).
2. `local.properties.example` → `local.properties`, поправить `sdk.dir`.
3. Всё: пароли keystore уже прописаны значениями по умолчанию.

---

## Договорённости, которые легко нарушить по незнанию

- **Ветки сведены в одну точку.** `claude/time-tracking-app-bxwjmh`,
  `claude/app-requirements-clarification-62ysfa` (она же ветка по умолчанию) и
  `claude/voice-parsing-todoist-button-rcfdti` смотрят на один коммит, поэтому
  свежий клон приезжает с актуальным кодом откуда угодно — раньше ветка по
  умолчанию показывала на версию 1.0 без Засечки, и новая сессия начинала
  работу поверх позапрошлого кода. Новая работа — своей веткой от этой точки, и
  потом свести обратно; PR не открывать без явной просьбы. `apk-builds` — не
  код, а один форс-коммит с APK.
- **Подпись** — только единым keystore (см. выше).
- **Прокси на VPS не делаем** (раздел 10 ТЗ): приложение ходит в Claude API
  напрямую, ключ живёт на телефоне.
- **Складывание Fold — больная тема.** Три раза чернота на 3–5 секунд
  приходила из-за синхронных обращений службы в чужие процессы и лишних
  оверлейных окон. Правила: ничего тяжёлого на главном потоке службы, окна
  не держать скрытыми (убирать из WindowManager), на оконные события не
  подписываться, за чужими окнами не подглядывать (опрос адресной строки
  Chrome удалён именно поэтому).
- **Лента — незаменимые данные.** Любое изменение `ZasechkaStore` проверяется
  вопросом «а если сюда прилетит пустой список?». Миграции пишут файл только
  после того, как записи прочитаны.
- **Полезность (★1–5) руками больше не ставится** — её заменила ценность часа.
  У старых записей оценка сохраняется как есть.

## Статус этапов

- [x] **Этап 0** — сборка, подпись, автоверсия, доставка APK
- [x] Этап 1 — каркас и Claude API
- [x] Этап 2 — служба доступности и системная кнопка
- [x] Этап 3 — плавающая кнопка
- [x] Этап 4 — Quick Tap и плитка
- [x] Этап 5 — словарь
- [x] Этап 6 — редактор промптов
- [ ] Этап 7 — Gemini Nano (не делаем: Сонет справляется, локальная модель не нужна)
- [x] Этап 8 — роутер, настройки, диагностика
- [x] Засечка 1.0 — лента, категории, автоматика, зеркала
- [x] Засечка 2.0 — ценность часа и балл дня, 24 часа, копии и восстановление,
      дела из Todoist
- [x] Разноска 1.0 — наговор → дела в Todoist: разбор Опусом, плашка,
      правка дела руками, защита от дублей и от потери разбора
- [x] Разноска 1.1 — решение по каждому делу на плашке, разбор чужого текста
      (буфер, выделение), маршруты на его поправках, отмена отправки
- [x] **Слушалка 1.0** — отдельное приложение: библиотека из папки на телефоне,
      плеер с обложкой и ±15 с, позиция с тиком раз в двадцать секунд,
      синхронизация мест между устройствами, вопрос по книге со спойлер-барьером,
      ответ вслух, пересказ «что было в прошлый раз»
- [x] **Слушалка 1.1** — читалка (шрифт, кегль, поля, междустрочье, четыре
      бумаги), картинки и карты из fb2/epub, переключение звук ↔ текст со
      сверкой по звуку, «что там было» по главам
- [x] **Слушалка 1.2** — разметка книги: пробы по каждому часу целиком на
      телефоне, карта файлом в папке книги (переезжает вместе с ней), после
      разметки переходы мгновенные и без распознавания
